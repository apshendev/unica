---
id: DEC.2026-08-20.ENGINES-COME-FROM-THE-TOOLCHAIN
status: superseded
governs: product
realized: crates/unica-bootstrap/tests/manifest_contract.rs::an_engine_is_accepted_from_the_toolchain_release
supersedes: []
superseded-by: DEC.2026-09-02.MAINTAINED-ENGINES-PUBLISH-AT-SOURCE
establishes: []
---

# Движок приезжает из тулчейна, а не из копии в выпуске плагина

**Решение.** В выпуск `unica` едет один артефакт — ядро. Движок доставляется из
релиза `unica-toolchain` по собственному тегу и собственному имени ассета;
адрес, тег, имя и сумма берутся из `third-party/tools.lock.json`, где они уже
записаны. Происхождение проверяется поартефактно: ядро обязано лежать в релизе
`unica` под тегом версии плагина, движок — в релизе `unica-toolchain` под тегом,
который назвал замок. Байты сверяются по `sha256` до распаковки, как и прежде.

**Почему.** Тулчейн уже публикует движки по цели, с суммами и происхождением,
а замок уже на него ссылается. Перепубликация тех же байтов в выпуске плагина
стоит 439 МБ на выпуск — `rlm-tools-bsl` 242, `bsl-analyzer` 177, `v8-runner`
20 — и даёт взамен ноль: сумма та же, источник тот же, владелец тот же.

**Второе, что дороже байтов.** Через копию доступность движка привязана к темпу
выпусков плагина: поднять `bsl-analyzer` нельзя без нового выпуска Unica, хотя в
тулчейне он уже опубликован. Разъединение делает версию артефакта тем, чем она
и объявлена, — его собственной.

**Что бережёт целостность.** Не единственность источника, а `sha256` в замке:
подмена байтов на любом из адресов кончается несовпадением суммы до распаковки.
Единственность источника защищала от опечатки в адресе, и поартефактная проверка
защищает от неё ровно так же — просто адресов теперь два, и оба названы. Сумма
доказывает целостность полученных байтов, но не создаёт доступность отсутствующего
или недостижимого релиза.

**Три оси, а не одна.** Происхождение решает роль: ядро собирается здесь, всё
прочее приезжает из тулчейна. Форма решает тип содержимого: архив
распаковывается целиком, одиночный файл кладётся под своим именем. Назначение
решает роль же: движок запускают, а поставку конфигурации отдают платформе.
Оси независимы, поэтому новый вид — расширение поставки, внешняя обработка —
объявляет роль и форму и не требует новых правил доставки.

Роль остаётся закрытым перечнем: молча принять незнакомую значит доставить
неизвестно что и неизвестно зачем.

**Цена.** Одобренных источников два вместо одного, и это правило теперь надо
охранять от расширения: третий адрес — новая запись, а не правка списка.
`unica-toolchain` добавляет самостоятельную область доступности на уровне
репозитория и релиза даже на том же GitHub: общий отказ провайдера коррелирован,
но удалённый или не опубликованный выпуск, права репозитория и ошибки публикации
отказывают независимо от выпуска плагина. Предварительный прогрев может заранее
подготовить кеш к закрытому контуру, но не является запасным источником и не
восстанавливает недоступный выпуск. И выпуск плагина перестаёт быть единственным
местом, по которому восстанавливается состав поставки: теперь их два, и связывает
их замок.
