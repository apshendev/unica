- Date: `2026-09-04`
- Status: `approved`
- Decision: `DEC.2026-09-05.TEST-PIPELINE-PLATFORM`

# Площадка под ярусы и сбор результатов

## Задача

Конвейер решает, что запускать, по одному признаку: какие файлы изменились.
Этого мало. Тест стоит денег каждый цикл, а доказывает он разное: одно
доказательство нужно на каждую правку, другое — раз в релиз, третье достаточно
получить однажды.

Документ описывает место, куда встанет второй признак, и намеренно не
расставляет ярусы по тестам: расстановка стоит дороже всей остальной площадки.
Заодно он отделяет от тестов то, что тестами не является, — исследования.
Заодно описывается то, без чего ярусы бессмысленны — как прогон оставляет
машинный результат и как результат попадает в отчёт на `main` и на релизной
линии.

## Что есть сейчас

Факты, снятые с дерева на `8729978d`:

- Rust: 4555 тестов, один вызов `cargo test --workspace -- --test-threads=1`.
  Локально весь набор идёт 354 секунды, на раннере — около четверти часа.
- Python: 55 модулей в трёх наборах, шесть команд внутри `verify-source`.
- Поиск: `v13_search_integration` вынесен `#[ignore]` и запускается своей
  джобой. Это единственный ярус в дереве — сделанный вручную и не названный.
- Демон: 47 тестов помечены `#[ignore]` с причиной и не гоняются нигде.
- Машинных результатов не производит ни один прогон. Гейт сверяет итоги **джоб**,
  а не тестов: он видит «джоба упала», но не видит, какой тест и почему.
- Сайт умеет принимать свежие результаты (`--line ИМЯ=КАТАЛОГ`), хранит их с
  историей и подписью прогона, но получать их пока неоткуда.

## Что говорят те, кто это уже прошёл

Четыре чужих измерения меняют замысел сильнее, чем любые рассуждения.

**Отбор тестов теряет отказы, пакетирование — нет.** Google на своём объёме
сравнил два способа сократить обратную связь. Отбор по изменённым файлам дал до
96% сокращения, но пропускал до 55% отказов. Пакетирование — прогон одной
пачки изменений вместо каждого по отдельности — дало до 99% сокращения и не
пропустило ни одного (Memon и др., ICSE-SEIP 2017). Вывод для нас: сначала
пакетировать, а умный отбор не строить вовсе.

**Отбор становится безопасным, когда учится на истории.** Meta заменила отбор
по зависимостям обучаемой моделью: стоимость вдвое ниже, при этом ловится более
95% отдельных отказов и более 99,9% ломающих изменений (Machalica и др.,
ICSE-SEIP 2019). Модели нужен большой корпус прошлых прогонов. У нас его нет —
и не будет, пока прогоны не оставляют машинных результатов. Это ещё один довод
делать эмиссию первой.

**Красный чаще означает шум, чем поломку.** У Google 84% переходов «прошёл →
упал» приходится на нестабильные тесты, 1,5% всех прогонов нестабильны, и лишь
1,23% тестов хоть раз находили поломку (Micco, ICST 2017). Два следствия.
Первое: красный гейт нельзя по умолчанию читать как «сломали код» — сегодня мы
на этом потеряли часы. Второе: раз подавляющее большинство тестов никогда
ничего не ловит, платить за них каждый цикл незачем — ярусы обоснованы.

**Размер теста — объективный признак, а не вкусовщина.** Google меряет тесты не
словами «модульный» и «интеграционный», а потреблением: `small` живёт в одном
процессе, `medium` может поднимать процессы и ходить только в localhost, `large`
работает с настоящим окружением. Признак проверяемый, и он же предсказывает
нестабильность. Наш ярус демона — ровно `medium`, и его нестабильность
характерна для класса, а не случайна.

**Двухступенчатый конвейер — рекомендация GitHub.** Дешёвые проверки на pull
request, полный набор — в очереди слияния, где прогон идёт против того
состояния, в котором изменение реально въедет. Это и есть пакетирование из
работы Google, доступное нам даром.

**Дешёвое — раньше дорогого.** GitLab советует ставить быстро падающее в начало
и отключать ненужное правилами, а не условиями внутри джобы.

## Две оси

**Размер** — `small`, `medium`, `large`. Признак объективный: поднимает ли тест
процесс, ходит ли в сеть, нужна ли ему платформа 1С.

Объявляется он **не в коде теста**, а в метаданных запуска — так это устроено у
Google и так же ложится на нас (разбор ниже). Умолчание — `medium`: неразмеченный
тест не должен по ошибке считаться дешёвым.

**Каденция** — как часто доказательство стоит повторять. Выводится из
размера, а не назначается вручную: чем дороже и нестабильнее класс, тем
реже. Никакого «раз и навсегда» среди каденций нет: то, что стоит доказать
однажды, — не тест, а `research`, и о нём отдельный раздел.

Контур (какие файлы изменились) остаётся третьим множителем и уже работает.

Смешать оси нельзя: контур меняется от коммита к коммиту, размер у теста
постоянен.

## Что тестировать и когда

| Ворота | Что гоняем | Почему так |
| --- | --- | --- |
| Pull request | `small` ∩ контур | Минуты обратной связи. Здесь платят за каждый коммит, поэтому только то, что живёт в одном процессе. |
| Очередь слияния | `small` + `medium` | Пакетирование: один прогон на пачку против будущего состояния `main`. Здесь ловится то, что отбор по файлам пропускает. |
| Push в `main` и релизную линию | `small` + `medium` | Отсюда собирается опубликованный отчёт. Пропуск здесь — дыра в том, что мы показываем публично. |
| Тег и ночной прогон | + `large` | Упаковка, установка, дым по опубликованным байтам. Ночью — только по линиям, чья вершина сдвинулась с прошлого такого прогона. |

Различие состояний в отчёте важно не меньше самого отбора:

- тест не выбран по каденции → `skipped`, причина «ярус не в этом прогоне»;
- тест отключён `#[ignore]` → `skipped`, причина автора;
- тест упал → `failed`, и это единственное состояние, красящее гейт.

## `research`: исследования — не тесты, и в конвейере их нет

Есть работа, похожая на тест, но тестом не являющаяся: доказать свойство,
которое не меняется от правок кода. Разобрать дамп платформы 8.3.27
побайтово. Сверить вывод с донором. Замерить поверхность против EDT-MCP.
Ответ у такой работы один, и получать его каждую ночь — платить за то, что
уже известно.

Это **исследование**, и у него есть имя в одном ряду с размерами тестов —
`research`. Ряд читается так: `small`, `medium`, `large` — тесты, которые
конвейер гоняет по своим воротам; `research` — то, что конвейер не гоняет
никогда. Исследование проводят один раз, результат закрепляют, и в workflow
оно не попадает ни в каком ярусе.

**Как проводится.** Вопрос сформулирован до начала. Метод воспроизводим:
команда, входы, версии инструментов и платформы — всё записано, чтобы
исследование можно было повторить намеренно, а не случайно. Результат
закрепляется в дереве артефактом — корпусом, снимком, таблицей — и записью в
реестре с датой, коммитом и методом: решение `DEC`, если из него следует
правило, или инвариант, если он его подтверждает. Так делается уже сейчас:
`scenario-corpus.json`, `donor-relations.json`, корпус 8.3.27 — все они
результаты исследований, а не тестов.

**Что остаётся тестам.** Страж, что закреплённый результат на месте и не
изменился случайно, — обычный `small`: он читает файл, а не проводит
исследование заново. Если закреплённое устарело, это повод для нового
исследования по решению, с новой записью, а не для ночного прогона.

**Следствие для дерева.** Сегодня несколько исследований живут в наборе
тестов под `#[ignore]` — «writes a retained source tree for the manual
8.3.27 ibcmd checkpoint», «writes an explicit developer-selected public-tool
XML corpus». По этой политике им место среди инструментов разработчика, а
не среди тестов — под именем `research`, например в `scripts/research/`:
тест, который никогда не должен исполняться в конвейере, — это не тест с
выключателем, а инструмент под другим именем.

Сделано 05.09.2026 (шаг 9): таких исследований в наборе оказалось три —
снимок дерева `cf init`, корпус XML публичного инструмента и регенерация
каталога событий платформы; сверка с донором и раньше шла инструментом
разработчика. Их код собирается только с фичей `research`, запуск — обёртками
из `scripts/research/`, правило закреплено инвариантом
`INV.TEST.RESEARCH-OUTSIDE-PIPELINE`.

## Шов: одна точка входа

Сейчас workflow сам знает, чем гонять: в нём написаны `cargo test`, `clippy`,
шесть команд `unittest`. Пока команда одна, ярус вписать некуда — пришлось бы
править YAML в шести местах и повторять отбор в каждом.

Поэтому появляется одна точка входа:

```
scripts/ci/run-tests.py --size <набор> --results <каталог> [--ecosystem rust|python]
```

Она знает про экосистемы, отбирает по размеру и складывает результат в один
каталог. Workflow перестаёт называть `cargo` и `unittest` — он называет скрипт.

Сегодня `--size all` делает ровно то же, что делают текущие команды: тот же
набор, тот же порядок, тот же цвет гейта. Отбор не включается. Смысл шага в том,
что **место для отбора появилось**.

## Статический анализ: та же цепочка, другой сток

Статический анализ у нас трёх родов, и они уже живут в дереве — только
вразброс. Разложить их по местам важнее, чем заводить под них workflow.

**Линтеры инструментов** — `cargo fmt`, `cargo clippy`, `py_compile`,
`check-version-contract.py`, реестровые `registry.py --check`, `fate.py`,
`immutability.py`. Сегодня `clippy` и `fmt` стоят **внутри тестовых джоб**, а
проверки Python — в `verify-source` рядом с тестами. Красный от линта и
красный от теста в гейте неразличимы, а линт ждёт сборки тестов, хотя мог бы
ответить первым.

Место линтеров — джоба `guards`, первая в цепочке и независимая от тестов:
GitLab называет это «дешёвое раньше дорогого». Одна оговорка, и она уже
записана в workflow: `clippy` зависит от цели — `#[cfg]` решает, какие
элементы существуют, — поэтому он остаётся на каждом раннере матрицы, но
своим шагом до тестов, а не между ними. Контур рулит и здесь: `clippy` — когда
менялся Rust, `py_compile` — когда Python, линтер workflow — когда `.github/`;
реестровые проверки — всегда, они стоят секунды.

Результат линтера — не результат теста. Это **находка**: файл, строка,
правило. У GitHub для находок есть штатный сток — Code Scanning через SARIF,
и он показывает их прямо в диффе pull request. `clippy` пишет SARIF через
`clippy-sarif`, линтер workflow `zizmor` — сам; CodeRabbit уже гонял его по
нашему YAML и нашёл запись в `GITHUB_PATH`. В Allure находкам делать нечего:
это отчёт о тестах, и линт в нём был бы ни `passed`, ни `failed`.

**Стражи-тесты** — 42 модуля Python, читающие дерево: граница платформы,
заголовки замыслов, покрытие реестра, интерполяция в `run:`. Это настоящие
тесты в форме `unittest`: детерминированные, один процесс, без сети — `small`
по определению. Они идут в профиле `small` через свой класс результата и
ложатся в Allure набором `guards`. Отдельного места им не нужно: они и есть
первая, самая дешёвая часть набора тестов.

**CodeQL** — единственный по-настоящему отдельный workflow, и он не наш:
GitHub гоняет его настройкой по умолчанию для `actions`, `python` и `rust`,
на pull request и раз в неделю. Оставляем как есть. Его сток — тот же Code
Scanning, так что все находки собираются в одном месте.

Итог: два стока по природе результата. Тест даёт исход — он едет в Allure.
Линтер даёт находку — она едет в Code Scanning. Один workflow, одна цепочка,
и `guards` в её начале.

## Результаты: почему не адаптер Allure

Отчёт собирает Allure, и метки — единственный способ показать в нём размер,
область и причину пропуска. Метка живёт только в `allure-results`: массив
объектов `{name, value}` в файле `{uuid}-result.json`, где имена `suite`,
`parentSuite`, `subSuite`, `epic`, `feature`, `story`, `tag`, `severity`,
`owner` управляют навигацией. В JUnit XML места для метки нет.

У Allure есть адаптеры, пишущие этот формат прямо из движка тестов, и они
закрывают вопрос меток целиком. Для обеих наших экосистем адаптер существует —
цена у них разная.

**Rust: `allure-cargotest`.** Официальный, Apache-2.0. Требует `#[allure_test]`
рядом с `#[test]` на каждом тесте — у нас их 4555 — и объявляет метки вызовами
внутри тела теста. Версия 1.5.0 вышла 27 августа 2026 года.

Проверен на деле, а не по описанию: пробный крейт с шестью тестами — обычный,
падающий, `#[ignore]` с причиной, `#[should_panic]`, возвращающий `Result`,
под `#[cfg(unix)]` — на Rust 1.98.0, nextest 0.9.143.

- Под `cargo test` всё компилируется и работает: пять файлов результата с
  метками `suite`, `parentSuite`, `historyId`, сообщением падения. Но файлов
  пять, а тестов шесть: **проигнорированный тест не записан**. Это не ошибка
  версии, а устройство: макрос оборачивает тело теста, а тело `#[ignore]`
  никогда не исполняется — записать его некому.
- Под `nextest` с процессом на тест — **один файл на пять тестов, и он не
  разбирается как JSON**. Имя файла составлено из метки времени и счётчика
  внутри процесса; пять процессов, стартовав в одну миллисекунду, выбрали
  одно имя и писали в него наперебой.

Итог: сегодняшний адаптер несовместим с процессом на тест и по построению не
видит отключённые тесты — а именно эти два свойства нам нужны первыми. Когда
имена файлов станут уникальными, а nextest — поддержанным, аннотация 4555
тестов делается кодмодом за один заход, и вопрос стоит открыть заново.

**Python: `allure-pytest`.** Зрелый и официальный, но наши 55 модулей —
`unittest`. Pytest их соберёт, а вот работают ли декораторы Allure на
`unittest.TestCase` в полном объёме, документация не утверждает. Плюс это смена
исполнителя набора ради формата вывода.

Поэтому решение такое: **nextest исполняет, JUnit — его внутренний вывод, а
результат для отчёта пишем и обогащаем мы, внутри джобы.** Оно принято после
пробы, а не до неё.

Что показала синтетика на 26 тестах из трёх источников, собранная Allure
2.46.1:

- JUnit Allure читает сам, механически всё работает. Но тесты из JUnit приходят
  голыми: метки только `resultFormat`, `suite`, `testClass`, `package`. Ни
  `size`, ни `language`; `report-name` из `<testsuites>` выброшен. По размеру
  такие тесты не отфильтровать.
- Дерево двоится: тесты из JUnit ложатся верхним уровнем по имени двоичного
  файла, снаружи ветки `rust`, куда легли тесты из JSON.
- Паника вне assert из JUnit приходит `failed`, не `broken`: nextest пишет
  `<failure>` на любую панику.
- Причину `#[ignore]` nextest не отдаёт ни в JUnit, ни в `list --message-format
  json` — там только `"ignored": true`. Причина есть в самом атрибуте.
- Адаптер `allure-cargotest` под `cargo test` даёт метки и дерево, но не даёт
  ни `broken`, ни отключённых, ни размера — и не работает под nextest.

Путь:

1. **Rust** — `cargo nextest` запускает каждый тест отдельным процессом и
   пишет JUnit с трейсами. Тут же, в `run-tests.py`, шаг перевода собирает
   `{uuid}-result.json` на тест из трёх входов: статус, время и трейс — из
   JUnit; полный путь и принадлежность к двоичному файлу — из `nextest list`;
   причина `#[ignore]` — из атрибута в исходнике, конструкции языка. Он же
   дописывает метки: `language`, `parentSuite`/`suite`/`subSuite` по пути
   модуля, `size` по профилю, ветку и коммит, и переводит панику вне assert в
   `broken` там, где это различимо по тексту.
2. **Python** — `unittest` принимает свой класс результата; он пишет тот же
   формат напрямую, без JUnit и без зависимостей. Прототип на четыре десятка
   строк собран и проверен в той же пробе.
3. **Сайт** — складывает `allure-results` джоб и зовёт `allure generate`.
   Читать ему нечего, кроме того, что уже в формате.

Так у двух языков в одном отчёте равные права: одно дерево, одни метки, один
набор состояний. Артефакт джобы — `allure-results`, самодостаточный: его
прочитает и сайт, и локальный `allure serve`.

Метки уровня прогона — ветка, коммит, раннер — идут штатными файлами
`environment.properties` и `executor.json`; категории `categories.json`
раскладывают пропуски на три полки. В пробе все пять полок легли по своим
признакам.

## Нестабильность лечится повтором, а не терпением

При 84% ложных переходов «прошёл → упал» повтор перед покраснением гейта — не
попустительство, а статистика. `nextest` умеет повторы с задержкой, а Allure
сам помечает тест `flaky`, если тот падал в последних пяти прогонах и с тех пор
проходил. История для этого уже лежит на сайте, и график повторов не даёт
нестабильности спрятаться: гейт зеленеет, но счётчик растёт на виду.

Красный гейт при этом остаётся красным для настоящего отказа.

Замер 05.09.2026, шаг 7. Параллельность по числу ядер: nextest на
ubuntu-latest — 286 → 138 с, на macos-14 — 444 → 132 с (4 495 и 4 502 теста),
локально на 14 ядрах — 395 → 120 с; ни одного нового падения. Повторы
включены стартовым числом: один повтор с паузой 2 с, и каждая неудачная
попытка уходит в отчёт записью с тем же именем — Allure складывает их
повторами последней, график повторов и метка `flaky` их видят. Поднимать
число — только по этому графику.

## Передача результатов на сайт

Джоба с тестами кладёт `results-<джоба>` артефактом — это `allure-results`
и план прогона; JUnit наружу не выходит. `unica-pages.yml` просыпается по `workflow_run`, забирает
артефакты прогона-источника, складывает их в один каталог и передаёт как
`--line <ветка>=<каталог>`. Сайт ничего не переводит — только складывает. Дальше
работает написанное: `build-site.py` собирает отчёт, кладёт рядом сырые
результаты, сводку и подпись прогона, а следующий прогон забирает историю с
сайта.

**Почему брать чужой артефакт здесь безопасно.** В общем случае это способ
протащить содержимое: в `workflow_run` от pull request артефакт кладёт автор
PR. Джоба admits только прогоны с `event == 'push'` и совпадающим
`head_repository`. Push в `main` или релизную линию — содержимое, уже прошедшее
pull request. Проверка стоит и работает; артефакты начнут читаться внутри неё.

**Два деплоя на один push.** Push сам пересобирает сайт и он же запускает
прогон, чей `workflow_run` пересоберёт сайт второй раз — первый без свежих
результатов, второй с ними. Очередь их разводит, стоит это около двадцати
секунд. Когда эмиссия появится, прямой триггер `push` можно снять.
*Снят 07.09.2026:* в очереди без отмены новый push заменял бы ожидающий
прогон с результатами, и они не доехали бы до сайта никогда.

**Релизная линия** идёт тем же путём без единой особенности.

**Отчёт линии — объединение ярусов** (шаг 8). Сайт хранит результаты линии
по профилям: `data/<линия>/results/<профиль>.tar.gz`. Отчёт собирается из
объединения: по тесту и раннеру (`historyId`) побеждает поздняя запись, и все
записи выигравшего прогона с этим ключом идут вместе — попытки повторов не
теряются. Push обновляет свою часть, ночной `large` — свою, и ни один не
стирает другую; ночь с пустым ярусом ничего не меняет в отчёте, кроме памяти
о проверенной вершине. Источником для сайта служит и красный прогон: страница
обязана показать то, что было; не источник только отменённый. Прогоны по
расписанию и по ручному запуску — тоже источники: у форка нет ни того, ни
push сюда.

## Упавший раннер не должен стирать тесты из истории

Раннер может умереть посреди прогона: отмена, срок, отказ инфраструктуры.
Джоба без артефакта — это тесты без результата, и в отчёте они просто исчезнут.
История каждого из них получит дыру, а читатель решит, что тесты убрали. Это
ложное утверждение, и его надо исключить.

**Статус.** Allure определяет пять состояний. `unknown` — «интеграция не
поставила статус, вероятно ошибка интеграции»; `broken` — «тест дефектен».
Оба лгут о том, что произошло. `skipped` — «тест был в плане, но не
исполнен, по любой причине» — это ровно наш случай. Поэтому тест, до которого
раннер не дошёл, попадает в отчёт как `skipped` с `statusDetails.message`
вида «раннер не дошёл: <джоба>, <исход>, <ссылка на прогон>». Он серый, не
красит гейт и не попадает в `flaky`: та метка считает только `failed` и
`broken`.

Так в отчёте живут три разных `skipped`, и различает их сообщение: не выбран
по каденции, отключён автором, не дождался раннера. Файл `categories.json`
выделяет третий в отдельную категорию «Инфраструктура» по сообщению — и в
отчёте он читается отдельной полкой, а не растворяется среди пропусков.

**Откуда взять список.** Чтобы дописать пропавшее, надо знать, что должно было
пройти. Джоба первым шагом выписывает план — `cargo nextest list --profile
<ворота>` и список Python — и выгружает его артефактом **до** запуска тестов:
шаг выгрузки, отработавший до смерти раннера, свой артефакт оставляет. Сайт
сверяет план с результатами каждой джобы и дописывает недостающее.

Если раннер умер раньше плана, план берётся у сайта (сделано 07.09.2026:
состав — записи последнего хранимого прогона того же профиля и раннера,
тесты пишутся `skipped` с причиной и побеждают старые записи в
объединении): состав прошлого прогона
этой линии он хранит. Тесты, добавленные тем самым коммитом, при этом
потеряются — это честная граница, и она много уже, чем «пропало всё».

## Отключённый тест обязан быть видимым

47 тестов яруса демона несут `#[ignore]` с причиной и обязаны стоять в отчёте
`skipped` с этой причиной: молча вынести тест из гейта нельзя.

`nextest` по умолчанию проигнорированные тесты в отчёт не пишет
(`junit.report-skipped = "none"`), но принимает значение `"ignored"` — «выводить
те, чей статус игнорирования не совпал с отбором прогона». Без этой настройки
переход на nextest тихо съел бы 47 строк отчёта.

Рядом лежит `junit.store-failure-output`, по умолчанию `true`. Отчёт публичный,
и вывод упавшего теста попадёт на сайт вместе с путями раннера. Оставляем как
есть: раннеры публичные, секретов в проекте нет, а вывод упавшего теста — самое
полезное, что есть в отчёте.

## Что делается в этом шаге

1. `scripts/ci/run-tests.py --size all`, повторяющий текущее поведение один в
   один. Workflow переводится на него.
2. Rust переходит на `cargo nextest`: JUnit, процесс на тест,
   `report-skipped = "ignored"`.
3. Класс результата `unittest`, пишущий `allure-results` напрямую.
4. Шаг перевода в `run-tests.py`: JUnit + `nextest list` + атрибут `#[ignore]`
   → `allure-results` с метками; тот же список — план для защиты от упавшего
   раннера.
5. Загрузка `results-*` артефактами и чтение их в `unica-pages.yml`.
6. Повторы для нестабильных классов и проверка, что Allure помечает `flaky`.

После этого на сайте появляется настоящий отчёт `main`, а ярусы включаются
правкой отбора в одном скрипте.

## Порядок приведения: десять шагов, каждый проверяем сам по себе

Правило одно на все шаги: гейт остаётся того же цвета, поведение меняется
ровно в одном месте, и у шага есть способ доказать, что он сделал то, что
обещал. Ярусы не расставляются до самого конца — сначала площадка, на
которой расстановку можно будет измерить.

| № | Шаг | Что меняется | Как проверяется | Что может сломаться |
| --- | --- | --- | --- | --- |
| 0 | Замысел принят | Этот документ переводится в `approved`, решение — записью `DEC` в реестре | Стражи замыслов и реестра зелёные | Ничего: кода нет |
| 1 | Одна точка входа | `scripts/ci/run-tests.py --profile all` повторяет сегодняшние команды один в один; workflow зовёт скрипт вместо `cargo test` и `unittest` | Тот же цвет гейта; число тестов и порядок совпадают с прежним выводом | Ничего по замыслу — это и есть проверка |
| 2 | nextest | `.config/nextest.toml`, JUnit, `report-skipped = "ignored"`, **`test-threads = 1`** — процесс на тест, но по одному; nextest ставится фиксированной версией с кэшем | 4 555 тестов в списке, 47 + 4 в `skipped`; время замерено и записано | Скрытая связь тестов через процесс: раньше они делили состояние, теперь нет. Ловится сразу, потому что параллельности ещё нет |
| 3 | Эмиссия | Класс результата для `unittest`, перевод JUnit + `nextest list` + атрибут → `allure-results`; `plan.json`, `run.json`; артефакт `results-<джоба>` с `overwrite: true`; раннер — параметр записи и часть `historyId` | Артефакт скачивается, `allure generate` локально: счёт равен плану, у отключённых причина автора, у двух раннеров два теста | Расхождение счёта с планом — его и ловит страж эмиссии |
| 4 | Сайт читает результаты | `unica-pages.yml` забирает `results-*` прогона-источника под действующей проверкой доверия, складывает по линиям, дописывает недошедшие по плану; `categories.json`, `environment.properties`, `executor.json`; сборка плагина запускается и по push в `main` и релизную линию — ворота линии, прогон-источник сайта | Первый настоящий отчёт `main` на сайте; второй прогон несёт историю первого | CodeQL отметит чтение артефакта — ответ уже написан в замысле и в workflow |
| 5 | `guards` и Code Scanning | Проверки Python, реестра и версии — в джобу `guards` первой; `clippy` — первым шагом Rust-джоб; `zizmor` на `.github/`; SARIF в Code Scanning; ожидания гейта обновлены | Таблица гейта видит `guards`; во вкладке Security появляются категории `clippy` и `zizmor` | Гейт: новая джоба должна быть в ожиданиях, иначе он красный по устройству |
| 6 | Профили без отбора | Профили `pr`, `queue`, `main`, `release` с `default-filter = 'all()'` и сроками по размеру; таблица «набор Python → размер», пока всё `small`; страж состава | Поведение прежнее, страж состава зелёный — площадка есть, отбора нет | Ничего: фильтр пропускает всё |
| 7 | Параллельность и повторы | `test-threads` с единицы на умолчание; замер; повторы только после первых прогонов с историей | Время падает; новых падений нет; повторы видны на графике Allure | Тесты, делящие ресурс машины, — порт, каталог. Именно их и ищем |
| 8 | Ночной `large` и память | `unica-nightly.yml`: перечисление открытых линий, сравнение с `profiles/large.json` с сайта, матрица по сдвинувшимся; прогон по тегу пишет ту же память (снято 07.09.2026, см. ниже); стражи «тег → линия» | Ручной запуск показывает перечисление и пропуск неподвижной линии; профиль `large` пока пуст и честно гоняет ноль | Ничего: пустой профиль |
| 9 | `research` из тестов | Четыре исследования под `#[ignore]` переезжают в `scripts/research/`; политика — записью в реестре | Набор тестов меньше на четыре, стражи зелёные, исследования запускаются руками | Ничего: они и так не гонялись |
| 10 | Расстановка размеров | Первый проход по структуре: интеграционные цели и демон — `medium`, сокетные модули по пути; страж размера | Профиль `pr` меньше `main` на измеренное число; отчёт показывает `size` | Это первый шаг, где отбор что-то пропускает. Потому он последний |

Зависимости: 1 → 2 → 3 → 4 — цепочка от шва до первого отчёта. Шаг 5
независим от 2–4 и может идти сразу после 1. Шаг 6 после 2, шаг 7 после 4 —
чтобы повторы было где увидеть, шаг 8 после 4 и 6. Шаг 9 независим от всех и
может идти первым. Очередь слияния — отдельное решение после шага 10.

Ни один шаг не требует Windows и не возвращает ярус демона: и то и другое
ждёт своих задач.

*Итог 07.09.2026.* Все десять шагов влиты (#723–#738), очередь слияния
включена (#739, #740), разбор старого workflow после площадки — #742,
#744–#747; Windows вернулся в ночной ярус. Решение
`DEC.2026-09-05.TEST-PIPELINE-PLATFORM` переведено в `active`, свидетельства —
стражи состава и размера в `tests/ci`. Решение
`DEC.2026-09-04.PUBLISHED-RUN-SHOWS-EVERY-TEST` им заменено: история
считается от имени теста и раннера, а не только от имени, и отбор по ярусу
виден не статусом `skipped`, а составом отчёта — сайт складывает профили
`main` и `large`, и тест, ушедший в другой ярус, остаётся в отчёте с
результатом своего яруса.

*Долги после площадки, 07.09.2026.* Code Scanning держал 96 находок zizmor,
и ни одна не красила гейт — сведены к нулю (#752): чужие действия закреплены
хешем коммита с версией в комментарии, свои идут по ветке, Dependabot
двигает хеши, `checkout` не оставляет токен, имя раннера идёт через env;
шесть находок приняты с причиной в комментарии, вычищенное держит страж
`tests/ci/test_action_pins.py`. В джобе тестов Rust появился кэш
зависимостей (#753): пишут push в ветку и ручной запуск, pull request и
очередь читают. Замер на одном дереве, профиль `main`, холодный и тёплый
прогоны: macOS 663 → 386 с (clippy 168 → 59, сборка тестов 263 → 137),
ubuntu 350 → 292 с (71 → 49, 110 → 99); в pull request джоба Rust —
ubuntu 4:49 → 3:27, macOS 7:53 → 4:25. Красный Windows ночного яруса
получил владельца — задача #750.

*Ревизия по независимому аудиту, 07.09.2026.* Слой памяти сайта был самым
тонким местом: четыре пути молчаливой потери из шести находок жили в нём.
Закрыто одной правкой: чтение с сайта различает 404 и сбой (`site_fetch.py`),
раннер без плана берёт состав у сайта, тег больше не пишет память `large`,
страница статуса всегда держит `main` и не заводит карточку тегу, список
джоб читается страницей в сто и сверяется со счётом, прямой триггер `push`
сайта снят. Шестая находка — стражи workflow сравнивали байты YAML, а не
структуру — закрыта следом: `tests/ci/test_unica_workflow.py` разбирает
workflow как YAML и проверяет триггеры, `needs`, условия, шаги, `with` и
скрипты; перенос строки или стиль списка стража больше не красят, а
текстом остались только комментарий версии у пина и запрещённые маркеры.

## Чего в этом шаге нет

- Расстановки размеров по тестам: цена ошибки здесь выше, чем у всей остальной
  площадки.
- Очереди слияния. Она даёт пакетирование, но включается отдельно и меняет
  правила слияния для всех.
- Возврата яруса демона и Windows.
- Правок гейта. Он продолжает сверять итоги джоб; читать результаты тестов
  осмысленно, только когда размеры расставлены.

## Где объявляется размер

У Google размер стоит **в файле сборки**, а не в исходнике теста: атрибут
`size` правила Bazel со значениями `small`, `medium`, `large`, `enormous` и
умолчанием `medium`.

Главное в их практике — не место, а то, что объявление **исполняется, а не
советует**. `small` не получает ни сети, ни базы, ни файловой системы и живёт в
одном потоке; `medium` ходит только в localhost; `large` не ограничен. К этому
привязаны сроки — 60, 300 и 900 секунд — и планирование: сборка не запускает
разом столько тяжёлых тестов, чтобы забить машину. Категории измеримы, а не
на глаз. Целевая пропорция у них примерно 80% small, 15% medium, 5% large.

Другие экосистемы кладут метку в код — маркеры pytest, `@Tag` в JUnit, наш
собственный `#[ignore]`. Это локально и не расходится с тестом, но требует
тронуть каждый тест и ничего не проверяет.

**Для нас подходит внешний путь**, и инструмент под него уже есть. У `nextest`
профили в `.config/nextest.toml` принимают `default-filter` — выражение над
пакетом, двоичным файлом, именем теста и платформой. Профиль на ворота
(`pr`, `queue`, `main`, `release`), выражение на размер — и ни один из 4555
тестов не меняется. У Python роль такого выражения играет разделение на
наборы `tests/ci`, `tests/arch`, `tests/dev`: оно уже примерно совпадает с
ценой.

Исполнение мы можем позволить себе наполовину и сразу: `slow-timeout` с
`terminate-after` в том же профиле даёт срок на тест — ровно то, чем Bazel
держит размер честным. Вторую половину, «`small` не поднимает процессов»,
проверяет страж: сегодняшний зонд с негодной паузой демона показал, что
принадлежность к классу определяется прогоном, а не глазами.

Замер 05.09.2026 на `main`: самый долгий Rust-тест — 16,4 с, p99 — 1,8 с; в
Python — 124 с у приёмочного прогона корпуса. Поэтому срок `small` (60 с,
остановка после второго периода) стоит в профиле `default` для всех, пока
размеры не расставлены; сроки `medium` и `large` придут переопределениями по
выражению размера. Ворота ложатся на одноимённые профили — `pr`, `queue`,
`main`, `release` — и все они пока выбирают `all()`.

Плата за внешний путь названа честно: выражение опирается на имена и пути,
поэтому переименование двоичного файла молча меняет состав яруса. Против этого
и нужен страж.

## Ночной прогон идёт только туда, где что-то изменилось

`large` гоняется по расписанию, и расписание не должно платить за
неподвижную ветку. `release-v0.12` живёт только ради хотфиксов и неделями не
получает ни одного коммита — прогонять по ней тяжёлый набор каждую ночь значит
получать один и тот же ответ за те же деньги. Правило: **ночной прогон
запускается по линии, только если её вершина сдвинулась с прошлого такого же
прогона.**

Две особенности GitHub делают это не тривиальным:

- расписание запускается только на ветке по умолчанию — по релизной линии оно
  не сработает вовсе. Поэтому ночной workflow один, на `main`, и сам перечисляет
  открытые линии тем же правилом, что и сайт (`site-status.py --print-lines`);
- у прогона по расписанию `head_branch` всегда `main`, даже когда он проверяет
  `release-v0.12`. Значит, линию нельзя брать из события — она едет в `run.json`
  артефакта, и сайт читает её оттуда.

Память о прошлом прогоне лежит там же, где всё остальное долговременное — на
сайте: `data/<линия>/profiles/<профиль>.json` с коммитом, временем и ссылкой.
Ночная джоба перечисления читает его с живого сайта, сравнивает с вершиной
линии и для каждой сдвинувшейся запускает `unica-large.yml` **на самой
линии** через `workflow_dispatch`: там `github.sha` и `github.ref_name` и
есть вершина и линия, checkout стандартный, и подпись артефактов не несёт
чужих значений. Так и CodeQL спокоен: checkout по значению из матрицы он
считал бы недоверенным кодом в контексте ветки по умолчанию. Ничего не
сдвинулось — ничего не запущено, и это пропуск по делу, а не отказ. Линия
без `unica-large.yml` площадки не несёт и в ночь не идёт — старая
`release-v0.12` так и остаётся в стороне, пока её не переведут.

Одна особенность GitHub закрывает цепочку иначе, чем хотелось бы: событие
завершения прогона, созданного `GITHUB_TOKEN`, другие workflow не будит —
защита от рекурсии (замер 05.09.2026: `large`, запущенный ночью, страницы не
разбудил, а такой же, запущенный рукой, — разбудил). Поэтому ночь дожидается
запущенных прогонов, забирает их артефакты и выкладывает своим: завершение
ночи страницы будит, её создал планировщик или человек. Сайт находит
`results-*` и `plan-*` с подписями на любой глубине.

06.09.2026 ночь стала тем, чем её называет таблица ворот. Вместо пустого
`unica-large.yml` она запускает на сдвинувшейся линии ручной контур сборки
плагина с входом `profile: large`: сборка инструментов, пакет, проба, сухой
RC, оценка — «упаковка, установка, дым». Тесты в этом прогоне идут профилем
`large`: на ubuntu и macOS он пуст до расстановки тяжёлых размеров, а на
Windows переопределением по платформе гоняет весь набор — с матрицы pull
request и `main` Windows снят за нестабильность раннера, ночью же его сбой
виден и никого не блокирует. Сайт принимает такой прогон как `large` и
пишет память о вершине; результаты Windows входят в объединённый отчёт
линии третьим раннером.

**Прогон по тегу память не пишет.** Каждый тег получает свой прогон профиля
`release`: десять тегов за день — десять прогонов, у каждого выпуска своё
доказательство по своим байтам. Но Windows живёт только в ночном ярусе, и
прогон тега его не гоняет; будь тег памятью, ночь после него пропустила бы
вершину, которую Windows не видел. Поэтому память пишет только `large`: ночь
после тега гоняет `large` на той же вершине и дописывает Windows в отчёт
линии, а дальше пропуск, пока не придут новые коммиты. «Относительно
последнего тега» правило не годится: оно переисполняло бы уже проверенную
вершину каждую ночь, пока её не отметят. *(До 07.09.2026 тег писал ту же
память, и ночь после него пропускала вершину без Windows.)*

**Теги только на релизных линиях** — практика, под которую схема ложится без
оговорок. Линия рождается веткой `release-vX.Y`, попадает в список открытых
и на сайт; тег `vX.Y.Z` на ней запускает `large`, и линия для его результатов
выводится из имени тега тем же правилом, каким `open_lines` сопоставляет
тег и ветку. `main` тегов не получает, и его `large` идёт только ночью по
сдвигу. Два стража держат это честным: тег, чьё имя не разбирается в линию,
— отказ, а не тихий `main`; тег на линии, у которой нет ветки, — отказ, а не
сирота без места на сайте.

Побочное следствие того же правила: ручной запуск по расписанной линии тоже
не переисполняет уже проверенный коммит, если его об этом не попросили явно.

GitHub отключает расписание после шестидесяти дней без коммитов в репозиторий.
Для нас это не угроза — `main` живёт, — но о нём стоит знать: если расписание
однажды перестанет срабатывать, причина скорее в этом, чем в YAML.

## Состав набора: измерено

| Что | Тестов | Доля |
| --- | --- | --- |
| Rust внутри крейтов | 4401 | 96% от Rust |
| Rust интеграционных (`tests/`) | 154 | 3% от Rust |
| Python: `tests/ci` | 759 | |
| Python: `tests/dev` | 256 | |
| Python: `tests/arch` | 122 | |
| **Всего** | **5692** | |

Признак `medium` по Google — процесс или сокет. Сканировались только
конструкции стандартной библиотеки и Cargo: `std::process`, `Command::new`,
`std::net`, `TcpStream`, `UnixStream`, `UnixListener`, `CARGO_BIN_EXE_`. Ни
одного нашего имени — на это есть отдельный раздел ниже.

Из 4539 объявлений `#[test]` внутри крейтов:

- **649 (14%)** лежат в файлах с запуском процесса;
- **2378 (52%)** лежат в файлах с запуском процесса **или** сокетом: протокол
  демона тестируется в процессе клиентом и сервером через localhost, и таких
  тестов много.

Обе цифры — верхняя оценка: признак файловый, а не тестовый, и файл с одним
сокетным тестом записывает в `medium` все свои тесты.

Первый разрез, из которого стоит начинать:

- `small` — от 2 100 до 3 900 Rust в зависимости от того, куда отнести
  сокетные тесты localhost, плюс большая часть Python;
- `medium` — остальное, включая 154 интеграционных, плюс приёмочные сценарии
  Python, поднимающие MCP;
- `large` — сегодня ноль: платформы 1С в конвейере нет.

У Google целевая пропорция 80/15/5. У нас по строгому счёту около 50/50 —
хуже, чем казалось по разделению «внутри крейта / интеграционный». Это
означает, что ворота pull request получат не «почти всё», а половину, и что
сокетные тесты localhost — главный кандидат на уточнение: по определению они
`medium`, по цене и стабильности часто ведут себя как `small`.

Первый проход сделан 05.09.2026 (шаг 10). Размер объявлен выражением в
`.config/nextest.toml`: `kind(test)` — интеграционные цели — плюс 57 термов
по модулям, в чьих файлах есть процесс или сокет; термы точны по тестам файла
(его встроенные модули и верхний уровень), тесты вложенных файлов ему не
принадлежат. Выражение пишет `scripts/ci/size-filters.py --write` из списка
nextest, а страж размера без `cargo` проверяет, что каждый файл дерева
модулей с процессом или сокетом в выражении назван, и что копии выражения —
у ворот `pr` и у срока `medium` — равны. Итог: `pr` — 2 069 из 4 502 тестов
(46%), `medium` — 2 433, из них 152 интеграционных; Python пока весь `small`.
Отчёт несёт метку `size` тем же правилом. Файл вне дерева модулей страж не
считает: у него нет ни размера, ни прогона.

Python получил свой разрез 06.09.2026: наборы остаются `small`, а
`.config/python-sizes.toml` поднимает до `medium` классы и отдельные тесты —
приёмочный корпус, два контракта продукта, дым MCP и паритет скриптов, всего
около 190 из 327 секунд набора. Признак тот же по духу, что у Rust: тест
зовёт `cargo` или поднимает сокет, то есть собирает или запускает продукт;
`subprocess` сам по себе признаком не стал — стражи реестра зовут `git` и
`python` и остаются `small`. Наш раннер `unittest` допускает в прогон только
размеры ворот, недопущенный тест в план не попадает, а страж держит записи
манифеста существующими и модули с `cargo` или сокетом — объявленными.
Ссылки реестра на эти тесты не менялись: файлы никуда не переезжали.
06.09.2026 растворилась джоба «Search integration»: поисковый тест
`canonical_search_is_source_scoped_and_rejects_legacy_call_shape` под
`#[ignore]` с маршрутом через флаг контура был ярусом под выключателем. Он
лежит в интеграционной цели, значит уже `medium` по `kind(test)`: выключатель
снят, отдельная джоба и её ожидание в гейте убраны, тест идёт в очереди и на
`main` обычным профилем. Два теста `issue_89_*` с тем же маршрутом в
джобе не запускались никогда — она гоняла только цель `v13_search_integration`
— и локально падают; они остаются под `#[ignore]` с честной причиной, а их
судьба — отдельная задача. Она решена тем же днём: оба теста `issue_89_*`
звали имена v0.12 (`unica.code.search`, `unica.meta.info`,
`unica.code.graph`), и их предмет — скрытый сервис RLM и анализатора — с
канонического провода недостижим, потому что `unica.search` ищет буквально
силами актора и провайдеров не поднимает. Цель `issue_89_workspace_service`
снята целиком, все четыре её теста: два `issue_89_*` и две самопроверки
обвязки этого же файла — чтение stderr с невалидным UTF-8 и снимок прогресса
поиска, — которые продукт не проверяли. Правила реестра `arch/` на цель не
ссылались — их держат модульные проверки в `workspace_services.rs`,
`workspace_index.rs` и `platform/process.rs`.
Флаг `search_integration_changed` остался: он питает `assessment_required`.

Тогда же снята джоба «Rust tests (macos-14 primary)» — экономия эпохи
контуров: rust-only pull request шёл на одном раннере. Под профилем `pr`
тесты идут полминуты, а сборку платишь всё равно; зато класс дефектов,
который решает `#[cfg]`, один раннер не видит. Любая правка Rust теперь
идёт полной матрицей, гейт и коллектор о `primary` не знают.

*Две полосы, 07.09.2026 (план под параллельные сессии, шаг 2).* Полная
матрица осталась там, где она решает: в очереди слияния, на push в ветку и
по кнопке. Pull request идёт только на ubuntu — быстрый сигнал; очередь
проверяет ровно то дерево, что ляжет в `main`, и класс `#[cfg]` ловит она
ценой одной выкидки из очереди, а не красного `main`. Метка `ci:full`
возвращает полную матрицу на pull request. Доля выкидок — под наблюдением
по метрикам.

## Страж размера не знает наших имён

Из чего складывается признак «поднимает процесс или ходит в сеть»:

- `std::process::Command`, `std::net::*`, `TcpStream`, `UnixStream` —
  стандартная библиотека Rust;
- `CARGO_BIN_EXE_<имя>` — переменная самого Cargo: он выставляет её на сборке
  интеграционного теста с путём к собранному двоичному файлу;
- `connect_owner_for_protocol_test` — **наша** функция в `interfaces/daemon.rs`.

Третье сканировать нельзя: переименование функции молча лишает стража
покрытия, и никто этого не заметит. Замер показывает, что и незачем — без
нашего имени теряется один файл с шестью тестами, стандартная библиотека и
Cargo находят остальные 66.

Но и скан по стандартной библиотеке — не исполнение, а подсказка: он смотрит
на файл, а не на тест, и не видит вызов через помощника. Поэтому страж
строится в два слоя, и ни один не зависит от наших идентификаторов:

1. **Структура.** Размер объявляется путём модуля в фильтре nextest, а страж
   проверяет пути — так же, как `test_rust_platform_boundary` держит
   OS-специфичный код за фасадом платформы. Правило вида «модуль, объявленный
   `small`, не импортирует `std::process` и `std::net`» — про стандартную
   библиотеку, не про нас.
2. **Исполнение.** Профиль `small` на Linux запускается внутри `unshare -rn` —
   без сети. Тест, потянувшийся к сокету, падает сам и сразу, что бы ни было
   написано в его исходнике и через какого бы помощника он ни шёл. Это тот же
   приём, которым Google держит `small` честным, в размере одной строки
   `run-tests.py`. Запуск процесса исполнением не запретить дёшево; для него
   остаются структура и ночной зонд.

## Перезапуск, повтор и матрица: когда один тест даёт два результата

Три ситуации, в которых у одного теста оказывается больше одного результата,
и в каждой отчёт обязан вести себя предсказуемо.

**Перезапуск джобы в GitHub.** Номер прогона остаётся прежним, растёт только
`run_attempt`. `actions/upload-artifact@v4` не перезаписывает артефакт с тем
же именем — загрузка второй попытки упадёт, если не сказать `overwrite: true`.
С ним побеждает последняя попытка, и это правильно: перезапускают, когда
первый результат не считают верным. `workflow_run: completed` срабатывает на
каждую попытку, поэтому сайт пересоберётся из последних артефактов, а
`run.json` несёт номер попытки. В истории Allure это два запуска на один
коммит — и так и должно быть, их действительно было два.

**Повтор внутри nextest.** Каждая попытка теста — отдельный файл результата с
одним `historyId`. Allure по этому признаку показывает последнюю попытку как
результат, а прежние — как повторы, и по истории ставит метку `flaky`. Ничего
не теряется и ничего не прячется: график повторов растёт на виду.

**Один тест на двух раннерах.** Здесь та же механика работает против нас:
`small` идёт и на ubuntu, и на macOS, и у одного теста оказываются два
результата с разных машин. Повторы Allure склеивает по полному имени теста и
параметрам, а не по `historyId` — проверено на 2.46.1: при разных `historyId`
и одном имени результат с ubuntu стал повтором результата с macOS, и отчёт
показал 5 709 тестов вместо 10 256. Поэтому раннер идёт в запись
**параметром** `runner`: в отчёте это один тест с двумя значениями параметра,
оба видны, ни один не спрятан как повтор. `historyId` при этом считается от
имени теста и раннера — история у каждой машины своя, по ней строятся тренд
и метка `flaky`.

## Жизнь конвейера, когда всё идёт не так

| Отказ | Что ломается | Противодействие |
| --- | --- | --- |
| GitHub Actions недоступен или очередь забита | Не едет ничего, PR не влить | Сделать нечего, но сайт не врёт: он показывает прошлый прогон с его подписью и датой, а не выдаёт его за сегодняшний |
| Упёрлись в предел параллельных раннеров | Джобы стоят в очереди | Матрица только там, где платформа важна; `concurrency` с отменой на pull request и без отмены на `main` |
| macOS-раннер дороже в разы | Счёт | `small` гоняется на ubuntu, macOS остаётся для контура платформы |
| Джоба зависла | Съедает шестичасовой предел GitHub | `timeout-minutes` на каждой джобе плюс `slow-timeout` внутри профиля: тест не переживает срок своего размера |
| Тест нестабилен | Красный не по делу | Повтор, метка `flaky` от Allure, график повторов; гейт краснеет только от настоящего отказа |
| Артефакт не загрузился | У сайта нет свежих результатов | Сайт собирает из хранимого; деградация видна по старой подписи прогона, а не прячется |
| Раннер умер посреди прогона | Часть тестов без результата | План выгружается до тестов; недошедшие дописываются `skipped` с причиной «раннер не дошёл» и своей категорией |
| Джобу перезапустили | Два артефакта с одним именем | `overwrite: true` — побеждает последняя попытка; сайт пересобирается по `workflow_run` заново; в истории два запуска на один коммит |
| Один тест на двух раннерах | Allure сочтёт их повторами и покажет один | Раннер — параметр записи `runner`: склейка идёт по имени и параметрам, оба результата видны; `historyId` от имени теста и раннера — две истории |
| Артефакт протух | Нечего пересобрать | Сайт хранит сырые результаты у себя — уже сделано |
| Сайт не отвечает при чтении хранимого | Профиль, история или память пропали бы из объединения молча | Только 404 значит «файла нет»; любой другой ответ останавливает сборку, прежний сайт остаётся (`site_fetch.py`) |
| Раннер упал раньше плана: сборка не прошла | Тесты раннера исчезают из отчёта, их место заняли бы старые записи соседнего профиля под свежей подписью | Состав берётся из последнего хранимого прогона того же профиля и раннера, тесты пишутся `skipped` с причиной и побеждают старые записи |
| Несколько push в `main` подряд | Промежуточный коммит остаётся без прогона: в группе очереди живёт один ожидающий, и GitHub отменяет его при появлении нового (05.09.2026: `d44cf7c2` отменён, когда за ним пришёл `e670e741`) | Так и задумано: побеждает новейшее состояние линии, сайт и гейт смотрят на него; `cancel-in-progress` только у pull request |
| Пересборка линии из хранимого | Тот же прогон ложится в историю второй раз: Allure не знает, что результаты старые (на сайте лежал трижды) | Перед пересборкой вершина перенесённой истории снимается — она и есть пересобираемый прогон; проверено на живых данных версией сайта |
| Деплой Pages упал | Сайт не обновился | Следующий push пересоберёт; очередь без отмены, чтобы не рвать деплой на середине |
| Инструмент недоступен при скачивании | Отчёт не собрать | У Allure стоит сверка по чек-сумме; `nextest` ставится фиксированной версией и кэшируется |
| Очередь слияния при медленном CI | Пропускная способность падает: полчаса на прогон и десять PR дают пять часов | В очередь идут только `small` и `medium`; `large` туда не попадает |
| Pull request из форка | Нет секретов и доступа к Pages | `small` от секретов не зависит; публикация идёт только с push в этот репозиторий |
| Сайт потерян целиком | Потеряна история и сырые данные | История производная и отрастёт; сырые данные последних дней остаются в артефактах прогонов |

Общее правило: **отказ инфраструктуры не должен превращаться в ложное
утверждение**. Конвейер может не доехать, но страница обязана показывать то, что
действительно было, с датой и коммитом.

## Открытые вопросы

1. **Порог готовности pull request.** Если очередь слияния не включается,
   `medium` нужно к чему-то привязать. Снятие черновика меняет состояние без
   нового коммита — надо проверить, даёт ли это событие, по которому запускается
   догоняющий прогон.
   *Решено 05.09.2026:* очередь слияния включена отдельным ruleset «merge
   queue» только для `main` — правило очереди не принимает шаблоны веток, а
   «protected branches» покрывает и `release-*`; релизные линии вливаются
   без очереди. Параметры: squash, до пяти pull request в одной сборке,
   слияние по одному, ожидание пять минут, срок проверки шестьдесят минут.
   Конвейер отвечает на `merge_group` профилем `queue` — полный набор, как
   push в ветку; на сайт прогон очереди не идёт. Так `medium` на `main`
   снова стоит до вливания, и порог готовности pull request не нужен.
   *Изменено 08.09.2026:* push в `main` тестов больше не гоняет, отчёт
   `main` идёт на сайт с прогона очереди — см.
   [2026-09-08-main-push-after-queue-design.md](2026-09-08-main-push-after-queue-design.md).
2. **Сколько повторов.** Повтор прячет настоящую поломку ровно так же, как
   лечит нестабильность. Число надо выбрать по своим данным, а данных пока нет —
   значит, включать повторы стоит после первых прогонов с историей.
   *Решено 05.09.2026:* первые пять прогонов с историей прошли без падений,
   стартовое число — один повтор с паузой 2 с; каждая попытка видна в отчёте,
   и поднимать число можно только по графику повторов.
