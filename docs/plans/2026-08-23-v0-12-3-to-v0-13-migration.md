# Unica v0.12.3 → v0.13 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Выпустить один ломающий v0.13, который заменяет опубликованную поверхность v0.12.3 на восемь предметных входов, обслуживает долгую работу через SEP-2663 либо три compatibility-инструмента и выполняет её в одном версионированном пользовательском демоне без потери изоляции worktree.

**Architecture:** Тонкий stdio-фронтенд остаётся единственным публичным MCP-сервером `unica`; все `tools/call` превращаются во внутренние Invocation и исполняются демоном. Демон держит WorkspaceActor на канонический worktree, устойчивые Invocation/Task, точный SharedWork и одну предметную сериализацию результата. Новая поверхность строится рядом со старой как непубличный профиль и включается только одним финальным переключением версии.

**Tech Stack:** Rust 2021, `rmcp 3.1.2`, Tokio, Serde/JSON, UUID v4, `fs2`, существующие platform-facade и atomic publication primitives, Python 3 CI probes, GitHub Actions, пакет Unica для Codex и Claude Code.

**Spec:** `docs/design/2026-08-23-v0-13-execution-surface-design.md`; `docs/design/2026-08-23-v0-13-module-contract-design.md`; `DEC.2026-08-23.V0-13-EXECUTION-SURFACE`; `DEC.2026-08-23.MODULE-CONTRACT`; baseline release `v0.12.3`; release umbrella issue `#581`.

## Global Constraints

- Источник сравнения — опубликованный `v0.12.3`, а не только `main`: baseline содержит 74 уникальных `unica.*`, включая шесть `unica.runtime.job.*`.
- До Task 22 текущий профиль v0.12 остаётся единственным профилем production-сборки. Внутренние тесты получают v0.13 через явный `SurfaceRelease::V13`; переменная окружения не может случайно опубликовать его.
- Публичный cutover атомарен: никакой сборки с 71 старыми и 8 новыми именами, никаких алиасов и никакого частичного снятия доменов.
- Один packaged binary `unica` сохраняется. Демон — скрытый режим того же бинаря, а не второй поставляемый executable и не второй MCP-сервер.
- SDK-типы `rmcp` живут только в `interfaces/` (`mcp.rs` и специализированных wire-проекторах). Application, daemon protocol и durable store используют собственные типы.
- Окно handoff равно 7 000 мс от приёма `tools/call`, включая admission. Известная долгая граница материализует Task сразу. Тесты управляют временем через fake clock и не спят семь секунд.
- Клиент с `io.modelcontextprotocol/tasks` видит 8 инструментов. Клиент без capability видит те же 8 плюс `unica.task.get`, `unica.task.result`, `unica.task.cancel`.
- `tasks/get`, `tasks/update`, `tasks/cancel` реализуются до объявления server Tasks capability. В v0.13 после материализации нет `input_required`; `tasks/update` возвращает стабильную ошибку `task_input_not_supported` для всех живых Task.
- Compatibility `task.result` — ограниченный wait, а не копия отсутствующего wire-метода `tasks/result`; один вызов удерживается не более остатка общего семисекундного окна.
- `OperationResult.job` и `OperationResult.work` не переносятся в v0.13 предметный результат. Task/compatibility receipt — transport projection поверх результата.
- Durable store не записывает raw arguments, токены, команды, URL с credentials или пути из пользовательского текста. Для восстановления допустимы только типизированные безопасные `ResumeDescriptor`.
- `apply` принимает непустой упорядоченный `ops[]`; `dryRun` и apply проходят одну валидацию, а весь пакет публикуется либо целиком, либо никак.
- Доставка остаётся core-first. `tools-download` не становится операцией `run`: engine delivery — SharedWork демона.
- `tests`, `features`, `log` и gRPC-профиль 8.5 не входят в v0.13. Они принадлежат v0.14/#563 или будущему platform profile.
- Публикация релиза не авторизуется этим планом. Task 23 подготавливает и доказывает release candidate; тег и marketplace publication выполняются отдельно по `docs/release-runbook.md`.

## TOGAF Gates and Dependency Order

Это Architecture Roadmap и Implementation/Migration Plan фаз E–F. Фаза G начинается только после принятия Gate F0.

```text
F0 contract closure
 └─ F1 reproducible v0.12.3 baseline
     └─ F2 Invocation + durable TaskStore
         └─ F3 daemon + WorkspaceActor + SharedWork
             └─ F4 Tasks/compatibility wire
                 └─ F5 eight domain contracts + parity
                     └─ F6 one public cutover + packaged migration matrix
```

Каждый gate заканчивается отдельным зелёным коммитом. Коммиты одного gate можно ревьюить независимо, но выпускать наружу разрешено только F6.

Перед первой Python-проверкой подготовить изолированное окружение из
зафиксированных CI-зависимостей и выполнять последующие `python3` команды после
его активации:

```bash
uv venv --python 3.12 .build/v013-plan-venv
uv pip install --python .build/v013-plan-venv/bin/python -r tests/ci/requirements.txt
source .build/v013-plan-venv/bin/activate
python3 -c 'import lxml, yaml, tree_sitter, tree_sitter_rust'
```

---

## Task 1: Lock the Approved Contracts in a Hidden Catalog (Gate F0)

**Files:**

- Create: `crates/unica-coder/src/application/v13/mod.rs`
- Create: `crates/unica-coder/src/application/v13/tool_catalog.rs`
- Modify: `crates/unica-coder/src/application/mod.rs`
- Modify: `crates/unica-coder/src/application/tool_contracts.rs`

- [ ] Add a failing Rust contract test that loads `SurfaceRelease::V13` and asserts these exact request shapes:

```text
view(at, filter?, limit?, cursor?)
apply(at, ops[1..], dryRun=false, ifRev?)
find(query, kind?, limit?)
search(query, scope?, regex=false, limit?)
check(at?, filter?)
diff(left, right, filter?, limit?, cursor?)
run(op?, args?)
docs(query, source?)
```

`left` and `right` are qualified logical addresses; `diff` rejects incomparable node kinds. `scope` is a logical subtree address. `run()` without `op` returns the operation dictionary. `jobId`, paths and provider identifiers are absent from all eight schemas.

- [ ] Assert the cross-tool boundaries in the same failing test:

  - `find` returns address candidates and never source bodies;
  - `search` returns content/symbol matches and does not become a node reader;
  - `check` validates persisted state; `apply(dryRun=true)` validates the proposed transaction through the same validator registry;
  - `diff` is read-only and cursor carries both source revisions;
  - `docs` filters source kinds, not provider identities;
  - operation names for `apply` live in node `can[]` data, not a deep union in `tools/list`;
  - `run` returns its operation skeletons as data and never exposes runner command lines.

- [ ] Run the red test:

```bash
cargo test -p unica-coder application::v13::tool_catalog -- --nocapture
```

Expected: failure because the v0.13 catalog and its eight schemas do not exist.

- [ ] Add `SurfaceRelease::{V12, V13}` and a hidden v0.13 catalog with the exact eight schemas above. `SurfaceRelease::from_package_version()` still resolves to V12; only tests may construct V13 explicitly. A V13 handler may return `not_implemented` in this task because the profile is unreachable in the package.

- [ ] Freeze the initial `run` dictionary in the contract as these typed intentions:

```text
source.create
source.attach
infobase.create
infobase.build
source.dump
source.convert
artifact.make
artifact.load
syntax.check
test.run
client.run
extension.sync
query.execute
```

`client.run` is terminal; a client intended to survive the call is a session and is rejected in v0.13. `test.run` remains in `run` for v0.13 and moves to `tests` only in v0.14. `tools-download` is deliberately absent.

- [ ] Encode the result envelope common slots in the hidden catalog test: `ok`, `at?`, `summary`, `data?`, `changed[]`, `warnings[]`, `diagnostics[]`, `artifacts[]`, `next[]`, `rev?`, `cursor?`. An empty slot is omitted; `set`, `sourceState`, `fileExists`, `job`, and `work` are forbidden.

- [ ] Run the green catalog test and prove the production registry is unchanged:

```bash
cargo test -p unica-coder application::v13::tool_catalog -- --nocapture
python3 tests/ci/test_tool_surface_ledger.py -v
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/application/v13 crates/unica-coder/src/application/mod.rs crates/unica-coder/src/application/tool_contracts.rs
git commit -m "feat(surface): lock the hidden v0.13 domain contracts"
```

## Task 2: Make the v0.12.3 Baseline Reproducible (Gate F1)

**Files:**

- Create: `scripts/ci/probe-unica-wire.py`
- Create: `tests/ci/test_probe_unica_wire.py`
- Create: `tests/fixtures/migration/v0.12.3-baseline.json`
- Modify: `scripts/ci/smoke-unica-mcp.py`
- Modify: `tests/ci/test_smoke_unica_mcp.py`

- [ ] Write failing Python tests for a `WireProbe` that can initialize a binary with an explicit protocol revision and Tasks capability, exhaust direct-first pagination, detect duplicate tool names, record response kinds and emit deterministic JSON.

- [ ] Add a test fixture process that returns two `tools/list` pages and make the test prove that notifications do not restart the aggregate deadline.

- [ ] Run the red tests:

```bash
python3 tests/ci/test_probe_unica_wire.py -v
python3 tests/ci/test_smoke_unica_mcp.py -v
```

- [ ] Extract the generic JSON-RPC/session code from `smoke-unica-mcp.py` into `probe-unica-wire.py`; keep process-tree cleanup and one aggregate deadline. The probe arguments are `--binary`, repeated `--binary-arg`, `--protocol-version`, `--tasks-capability on|off`, `--output`, and `--timeout-seconds`.

- [ ] Generate `v0.12.3-baseline.json` from the published release/tag and commit the observed evidence: release/tag identity, target, 74 sorted unique names, six sorted runtime job names, package form `full-runtime`, and whether the MCP process was reachable only after full archive installation.

- [ ] Add assertions that the fixture is immutable and names all six:

```text
unica.runtime.job.start
unica.runtime.job.status
unica.runtime.job.wait
unica.runtime.job.logs
unica.runtime.job.list
unica.runtime.job.cancel
```

- [ ] Run the live baseline probe in an isolated temporary cache for the current target and compare it to the checked fixture. Save generated runtime output under `.build/`, not `docs/`.

- [ ] Run:

```bash
python3 tests/ci/test_probe_unica_wire.py -v
python3 tests/ci/test_smoke_unica_mcp.py -v
python3 scripts/ci/probe-unica-wire.py --binary .build/v0.12.3/unica --protocol-version 2025-06-18 --tasks-capability off --output .build/v0.12.3-wire.json
```

- [ ] Commit:

```bash
git add scripts/ci/probe-unica-wire.py scripts/ci/smoke-unica-mcp.py tests/ci/test_probe_unica_wire.py tests/ci/test_smoke_unica_mcp.py tests/fixtures/migration/v0.12.3-baseline.json
git commit -m "test(migration): capture the v0.12.3 wire baseline"
```

## Task 3: Introduce the Interface-Neutral Invocation Model

**Files:**

- Create: `crates/unica-coder/src/domain/invocation.rs`
- Create: `crates/unica-coder/src/application/invocation.rs`
- Modify: `crates/unica-coder/src/domain/mod.rs`
- Modify: `crates/unica-coder/src/application/mod.rs`
- Modify: `crates/unica-coder/src/application/ports.rs`

- [ ] Add unit tests for the state machine before implementation:

```rust
Queued -> Working -> Completed(DomainResult)
Queued -> Working -> Failed(InvocationFailure)
Queued -> Working -> Cancelled
Inline -> MaterializedTask -> terminal
```

Illegal backwards transitions, a second terminal result, and materialization after a direct response must fail without mutation.

- [ ] Test that `DomainResult` serializes identically whether wrapped as `InvocationOutcome::Direct` or stored as a terminal task result.

- [ ] Run the red test:

```bash
cargo test -p unica-coder domain::invocation application::invocation -- --nocapture
```

- [ ] Implement interface-neutral types:

```rust
pub struct InvocationId(Uuid);
pub struct TaskId(Uuid);
pub enum InvocationStatus { Queued, Working, Completed, Failed, Cancelled }
pub enum InvocationOutcome { Direct(DomainResult), Task(TaskSnapshot) }
pub enum ResumeDescriptor { Delivery(DeliveryResume), Index(IndexResume), Provider(ProviderResume), Runtime(RuntimeResume) }
pub trait Clock { fn now(&self) -> Instant; }
```

Use an injected `ManualClock` in tests and a monotonic Tokio-backed clock in production. Do not derive an Invocation identity from raw arguments; store only the normalized arguments hash.

- [ ] Keep `OperationResult` as the current v0.12 adapter for now. Add `From<DomainResult> for OperationResult`, not the inverse, so new domain code does not depend on legacy slots.

- [ ] Run:

```bash
cargo test -p unica-coder domain::invocation application::invocation -- --nocapture
cargo test -p unica-coder application::tests -- --nocapture
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/domain/invocation.rs crates/unica-coder/src/application/invocation.rs crates/unica-coder/src/domain/mod.rs crates/unica-coder/src/application/mod.rs crates/unica-coder/src/application/ports.rs
git commit -m "feat(invocation): add one direct and task outcome model"
```

## Task 4: Add the Durable Task and Invocation Store

**Files:**

- Create: `crates/unica-coder/src/application/invocation_store.rs`
- Create: `crates/unica-coder/src/infrastructure/task_store.rs`
- Modify: `crates/unica-coder/src/application/mod.rs`
- Modify: `crates/unica-coder/src/infrastructure/mod.rs`
- Modify: `crates/unica-coder/src/composition.rs`

- [ ] Add failing tests for create-before-return, atomic status update, terminal result retention, expiry after `ttlMs`, no expiry of nonterminal records, and idempotent cancel.

- [ ] Add crash fixtures: a truncated temporary file is ignored and classified; a committed record survives reopening; a non-resumable `working` record becomes `failed/interrupted`; a resumable delivery record keeps the same TaskId.

- [ ] Add a secret-safety test with sentinel strings in arguments and assert none occurs anywhere below the store directory.

- [ ] Run red tests:

```bash
cargo test -p unica-coder infrastructure::task_store -- --nocapture
```

- [ ] Define `InvocationStore` as an application port and implement a sole-writer file store owned by the daemon. Persist one versioned record per task through private temp file, flush, atomic rename and directory sync where supported by the platform facade.

- [ ] Persist only: schema version, TaskId, InvocationId, tool name, normalized hash, workspace identity hash, timestamps, status, safe status message, poll interval, TTL, terminal `DomainResult`, and optional typed `ResumeDescriptor`.

- [ ] Keep this store separate from `application/result_store.rs`: that store handles bounded continuation of oversized typed reads, not execution lifecycle.

- [ ] Run:

```bash
cargo test -p unica-coder infrastructure::task_store -- --nocapture
cargo test -p unica-coder --lib application::result_store -- --nocapture
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/application/invocation_store.rs crates/unica-coder/src/infrastructure/task_store.rs crates/unica-coder/src/application/mod.rs crates/unica-coder/src/infrastructure/mod.rs crates/unica-coder/src/composition.rs
git commit -m "feat(tasks): persist invocation lifecycle safely"
```

## Task 5: Bootstrap One Versioned User Daemon

**Files:**

- Create: `crates/unica-coder/src/infrastructure/daemon/mod.rs`
- Create: `crates/unica-coder/src/infrastructure/daemon/identity.rs`
- Create: `crates/unica-coder/src/infrastructure/daemon/protocol.rs`
- Create: `crates/unica-coder/src/infrastructure/daemon/client.rs`
- Create: `crates/unica-coder/src/infrastructure/daemon/server.rs`
- Create: `crates/unica-coder/src/interfaces/daemon.rs`
- Modify: `crates/unica-coder/src/interfaces/mod.rs`
- Modify: `crates/unica-coder/src/infrastructure/mod.rs`
- Modify: `crates/unica-coder/src/main.rs`

- [ ] Add failing tests that race two frontend processes and assert one daemon record/PID, one loopback endpoint, successful token handshake, and separate endpoints for incompatible core identities.

- [ ] Add negative tests for wrong token, world-readable state directory, stale PID/record, endpoint owned by another core identity, oversized JSON line, and mismatched daemon protocol version.

- [ ] Run the red tests:

```bash
cargo test -p unica-coder infrastructure::daemon -- --nocapture
```

- [ ] Extract the already-hardened loopback TCP + random token + spawn-lock pattern from `infrastructure/workspace_services.rs`. Keep cross-platform loopback TCP for v0.13 rather than adding Unix sockets and Windows named pipes in the same migration.

- [ ] Add hidden `unica --daemon --state-root ... --core-identity ...`; the stdio frontend lazily spawns it through the existing platform process facade. The endpoint record lives below the private user/provider state root, keyed by daemon protocol version and immutable core identity.

- [ ] Implement only `ping`, owner lease and graceful shutdown in this task. No tool is routed yet and the public MCP surface cannot observe the daemon.

- [ ] Test idle lifecycle with a configurable short grace: frontend owner keeps the daemon alive; after the last owner disappears and no work exists, it shuts down and removes only its own endpoint record.

- [ ] Run:

```bash
cargo test -p unica-coder infrastructure::daemon -- --nocapture
cargo test -p unica-coder infrastructure::workspace_services -- --nocapture
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/infrastructure/daemon crates/unica-coder/src/interfaces/daemon.rs crates/unica-coder/src/interfaces/mod.rs crates/unica-coder/src/infrastructure/mod.rs crates/unica-coder/src/main.rs crates/unica-coder/src/infrastructure/workspace_services.rs
git commit -m "feat(daemon): add one versioned user executor"
```

## Task 6: Move Workspace Ownership Behind WorkspaceActor

**Files:**

- Create: `crates/unica-coder/src/infrastructure/workspace_actor.rs`
- Modify: `crates/unica-coder/src/infrastructure/workspace_services.rs`
- Modify: `crates/unica-coder/src/infrastructure/workspace.rs`
- Modify: `crates/unica-coder/src/infrastructure/workspace_state.rs`
- Modify: `crates/unica-coder/src/infrastructure/workspace_index.rs`
- Modify: `crates/unica-coder/src/infrastructure/daemon/server.rs`

- [ ] Write failing actor tests for serialization of mutations, concurrent reads, source revision fencing, distinct actors for two worktrees of one repository, and reuse for two frontends on the same canonical worktree.

- [ ] Add a multiroot provider fixture with identical relative module paths in roots A and B; prove results remain bound to the requesting root.

- [ ] Run red tests:

```bash
cargo test -p unica-coder infrastructure::workspace_actor -- --nocapture
```

- [ ] Extract `WorkspaceServiceRuntime` ownership into `WorkspaceActor`. Its identity contains canonical workspace root, canonical source-set roots and provider profile; it does not collapse to Git repository identity.

- [ ] Add a daemon actor registry keyed by `WorkspaceIdentity`. Keep the old per-workspace service CLI as a compatibility adapter into the actor until Task 22, so this refactor does not change the v0.12 surface.

- [ ] Route workspace revision, index session and provider root binding through the actor. Enforce source revision fence before publishing any write result.

- [ ] Run:

```bash
cargo test -p unica-coder infrastructure::workspace_actor infrastructure::workspace_services infrastructure::workspace_index -- --nocapture
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/infrastructure/workspace_actor.rs crates/unica-coder/src/infrastructure/workspace_services.rs crates/unica-coder/src/infrastructure/workspace.rs crates/unica-coder/src/infrastructure/workspace_state.rs crates/unica-coder/src/infrastructure/workspace_index.rs crates/unica-coder/src/infrastructure/daemon/server.rs
git commit -m "refactor(workspace): isolate worktrees behind actors"
```

## Task 7: Execute Every Hidden Canonical V13 Invocation in the Daemon and Enforce the 7-Second Handoff

Task 7 applies only to the explicitly injected `SurfaceRelease::V13` profile.
The pre-Task7 main profile has 71 production-v0.12 tools and keeps its exact
legacy dispatch until the atomic Task 22 cutover. The published v0.12.3 fixture
with 74 names remains a separate migration baseline. This task does not map the
legacy registry to eight ToolIdentity values, persist a legacy payload, or
deduplicate destructive legacy calls.

**Files:**

- Modify: `crates/unica-coder/src/application/invocation.rs`
- Modify: `crates/unica-coder/src/application/invocation_store.rs`
- Create: `crates/unica-coder/src/application/invocation_store_actor.rs`
- Modify: `crates/unica-coder/src/application/operation_descriptors.rs`
- Modify: `crates/unica-coder/src/infrastructure/daemon/protocol.rs`
- Modify: `crates/unica-coder/src/infrastructure/daemon/client.rs`
- Modify: `crates/unica-coder/src/infrastructure/daemon/server.rs`
- Modify: `crates/unica-coder/src/infrastructure/task_store.rs`
- Modify: `crates/unica-coder/src/interfaces/mcp.rs`

- [ ] Add a daemon protocol test for `SubmitInvocation`, `GetTask`, `WaitTask`, and `CancelTask`. Submission returns either `Direct(DomainResult)` or a durable `TaskSnapshot` and never starts a second execution during polling. The admission request may carry raw arguments and a workspace hint, but after cheap validation daemon replaces it with an opaque actor-bound invocation: the service sees no raw hint, and execution/publication use the exact actor root binding and revision fence. Persist only the closed schema-v2 Task allowlist with actor-derived identity and `SafeFailureReason`; never persist raw arguments, caller/runtime text, stdout/stderr or open failure text.

- [ ] Preallocate TaskId and register live ownership before atomically creating a materialized `Working` record; do not execute or disclose TaskId before the normal or uncertain create result matches the complete expected record. Resolve uncertain create/terminal/cancel commits by exact identity/state readback under a bounded monotonic reconciliation policy with exponential backoff. Retain the live owner and actor capability while retrying only store publication; never repeat domain execution or allow idle exit before a terminal record is confirmed. Route every executor store call through one bounded serial store actor; bound file-record size, retention and recovery, and publish creates atomically without replacement. If exact confirmation or store ownership remains impossible, close submit admission as `durability_uncertain`, hide staged output, close the listener and request process restart without claiming in-process resources released. Keep the old PID endpoint until process death; only then may a successor acquire the store, replace the stale endpoint and recover residual `Working` records.

- [ ] Recover v1/v2 `Working` records without a registered resume owner as closed interrupted/resume-unsupported failures, migrate v1 terminal records deterministically, and reject unknown schemas. Keep the actor registry weak, prune dead entries and reject the 65th simultaneously live actor with the closed retryable `workspace_capacity` code without evicting active actors.

- [ ] Add fake-clock boundary tests: completion at 6 999 ms is direct; unfinished work at 7 000 ms is already durably queryable; simultaneous completion/handoff produces exactly one terminal result and one execution count.

- [ ] Add known-long tests for missing engine, cold index, provider startup, occupied write lease and external process. They materialize immediately after cheap validation.

- [ ] Run red tests:

```bash
cargo test -p unica-coder application::invocation -- --nocapture
cargo test -p unica-coder infrastructure::daemon -- --nocapture
```

- [ ] Add `ExecutionClass::{InlineCandidate, KnownLong}` to operation descriptors. The class describes a proven boundary, not a hard-coded list of slow tool names.

- [ ] Start the deadline when the frontend receives `tools/call`. Pass remaining response budget to the daemon. If a known host deadline is earlier than 7 000 ms plus serialization margin, hand off earlier; never infer duration from an external timeout.

- [ ] Keep a one-way `DomainResult -> OperationResult` compatibility projection for the hidden canonical seam and allow it to project legacy `working` only until Task 22. Do not daemonize the current V12 registry in this task, add the inverse conversion, or repeat a canonical domain call to continue it.

- [ ] Run:

```bash
cargo test -p unica-coder application::invocation -- --nocapture
cargo test -p unica-coder infrastructure::daemon -- --nocapture
cargo test -p unica-coder interfaces::mcp -- --nocapture
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/application/invocation.rs crates/unica-coder/src/application/operation_descriptors.rs crates/unica-coder/src/infrastructure/daemon crates/unica-coder/src/interfaces/mcp.rs
git commit -m "feat(invocation): hand long calls off after seven seconds"
```

## Task 8: Implement SEP-2663 Without Publishing It Yet (Gate F4, Native Profile)

**Files:**

- Create: `crates/unica-coder/src/interfaces/task_projection.rs`
- Modify: `crates/unica-coder/src/interfaces/mcp.rs`
- Modify: `crates/unica-coder/src/application/invocation.rs`
- Modify: `crates/unica-coder/src/infrastructure/daemon/client.rs`

- [ ] Add failing wire tests based on ordinary `tools/call`: with Tasks capability a forced-long result is `CreateTaskResult`; without capability the same request never receives a native Task.

- [ ] Add tests for `tasks/get`, `tasks/cancel`, and `tasks/update`. `get` returns `DetailedTask`; `cancel` is idempotent; `update` returns `task_input_not_supported` because v0.13 never materializes an input-required task.

- [ ] Test that direct and terminal-task `CallToolResult` JSON are byte-for-byte equal after removing the protocol Task wrapper.

- [ ] Run red tests:

```bash
cargo test -p unica-coder interfaces::mcp::tests::tasks interfaces::task_projection -- --nocapture
```

- [ ] Implement `ServerHandler` task hooks with rmcp wire types only in `interfaces`. Do not use rmcp's in-memory `TaskManager` as source of truth; adapt daemon records to `Task`, `DetailedTask`, `CreateTaskResult`, `GetTaskResult`, and `TaskAckResult`.

- [ ] Advertise server Tasks capability only for injected `SurfaceRelease::V13`; the package version remains v0.12 until Task 22.

- [ ] Preserve progress notifications as optional UI output, but do not use their delivery as task state or success evidence.

- [ ] Run:

```bash
cargo test -p unica-coder interfaces::mcp interfaces::task_projection -- --nocapture
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/interfaces/task_projection.rs crates/unica-coder/src/interfaces/mcp.rs crates/unica-coder/src/application/invocation.rs crates/unica-coder/src/infrastructure/daemon/client.rs
git commit -m "feat(mcp): project durable invocations as SEP-2663 tasks"
```

## Task 9: Add the Three No-Tasks Compatibility Tools

**Files:**

- Modify: `crates/unica-coder/src/application/v13/mod.rs`
- Create: `crates/unica-coder/src/application/v13/task_tools.rs`
- Modify: `crates/unica-coder/src/application/mod.rs`
- Modify: `crates/unica-coder/src/application/tool_contracts.rs`
- Modify: `crates/unica-coder/src/interfaces/mcp.rs`

- [ ] Add failing profile tests for exact names: native profile has no `unica.task.*`; compatibility profile has exactly `get`, `result`, `cancel`; neither has `list`, `logs`, or `unica.runtime.job.*`.

- [ ] Test `get` immediate state, `result(waitMs)` bounded wait, `cancel` idempotency, expired task classification, daemon restart, and terminal result parity.

- [ ] Run red tests:

```bash
cargo test -p unica-coder application::v13::task_tools interfaces::mcp::tests::surface_profiles -- --nocapture
```

- [ ] Implement the three tools as adapters to the same daemon TaskStore. Validate `waitMs` to `0..=7000`; cap it by the remaining request window and return a normal compatibility receipt if still working.

- [ ] Select profile only from actual negotiated/request capability. Do not branch on host name or protocol-version string alone, and do not cache one client's tool list for another.

- [ ] Run:

```bash
cargo test -p unica-coder application::v13::task_tools interfaces::mcp -- --nocapture
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/application/v13 crates/unica-coder/src/application/mod.rs crates/unica-coder/src/application/tool_contracts.rs crates/unica-coder/src/interfaces/mcp.rs
git commit -m "feat(mcp): emulate tasks for clients without the extension"
```

## Task 10: Introduce Exact SharedWork and Migrate Core-First Delivery

**Files:**

- Create: `crates/unica-coder/src/application/shared_work.rs`
- Modify: `crates/unica-coder/src/application/ports.rs`
- Modify: `crates/unica-coder/src/infrastructure/engine_delivery.rs`
- Modify: `crates/unica-coder/src/infrastructure/platform/filesystem.rs`
- Modify: `crates/unica-coder/src/infrastructure/bundled_tools.rs`
- Modify: `crates/unica-coder/src/infrastructure/daemon/server.rs`
- Modify: `crates/unica-bootstrap/src/lib.rs`

- [ ] Add failing SharedWork tests: one producer/many followers, exact-key coalescing, different-key separation, producer failure fan-out, follower cancellation, last-owner cancellation, and no admission permit held while awaiting a producer.

- [ ] Define exact key variants:

```rust
Delivery { artifact, version, target, sha256, form }
Index { workspace, source_revision, provider, profile }
Provider { engine, target, capabilities }
Runtime { resource_identity, lease_identity }
```

- [ ] Run red tests:

```bash
cargo test -p unica-coder application::shared_work -- --nocapture
```

- [ ] Replace `deliver_engine_if_missing(...) -> Option<WorkState>` with a SharedWork request that yields `ArtifactReady` or a classified failure. Task 10 attaches the daemon-owned delivery capability to `ActorBoundExecution` and proves durable handoff before its wait; the production canonical subject handler is still dormant, so durable Invocation/Task progress projection is completed with the Task 22 handler cutover rather than claimed by this slice. The V12 compatibility projection continues to publish its existing progress shape.

- [ ] Preserve existing delivery invariants: pinned identity, SHA-256, verified file manifest, private staging, atomic publication, range resume only when supported, connect/read silence timeout, and `prefetch` of the closed offline contour.

- [ ] Add integration tests for two worktrees sharing an identical immutable delivery, different SHA-256 values not sharing, interrupted archive not becoming ready, and one follower cancelling without cancelling the producer.

- [ ] Run:

```bash
cargo test -p unica-coder application::shared_work infrastructure::engine_delivery infrastructure::bundled_tools -- --nocapture
cargo test -p unica-bootstrap --lib -- --nocapture
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/application/shared_work.rs crates/unica-coder/src/application/ports.rs crates/unica-coder/src/infrastructure/engine_delivery.rs crates/unica-coder/src/infrastructure/bundled_tools.rs crates/unica-coder/src/infrastructure/daemon/server.rs crates/unica-bootstrap/src/lib.rs
git commit -m "feat(delivery): join cold engines through shared work"
```

## Task 11: Migrate Index, Provider and Runtime Ownership to SharedWork

**Files:**

- Modify: `crates/unica-coder/src/application/shared_work.rs`
- Modify: `crates/unica-coder/src/domain/source_revision.rs`
- Modify: `crates/unica-coder/src/infrastructure/daemon/server.rs`
- Modify: `crates/unica-coder/src/infrastructure/engine_delivery.rs`
- Modify: `crates/unica-coder/src/infrastructure/platform/filesystem.rs`
- Modify: `crates/unica-coder/src/infrastructure/platform/mod.rs`
- Modify: `crates/unica-coder/src/infrastructure/platform/process.rs`
- Modify: `crates/unica-coder/src/infrastructure/runtime_jobs.rs`
- Modify: `crates/unica-coder/src/infrastructure/workspace_actor.rs`
- Modify: `crates/unica-coder/src/infrastructure/workspace_services.rs`
- Add: `arch/decisions/2026-08-24-long-work-ownership-slice.md`
- Add: `arch/invariants/INV.APP.EXACT-LONG-WORK-OWNERSHIP.md`
- Add: `arch/invariants/INV.APP.RUNTIME-RESOURCE-TREE.md`
- Add: `arch/contracts/CTR.APP.DAEMON-LONG-WORK-CAPABILITIES.md`
- Modify: `arch/index.md`
- Modify: `docs/plans/2026-08-23-v0-12-3-to-v0-13-migration.md`

- [ ] Add failing tests proving that the full trusted source revision and actor-safe identity are in the index key, provider root remains in every request/result route, a new revision never receives an old result, and one multiroot provider host may be reused without merging actor bindings or caches. Two distinct worktrees intentionally never share an Index key.

- [ ] Add runtime process-tree tests on Unix and Windows abstractions: Task remains working while a grandchild lives, cancellation terminates the owned tree, and resource lease is released only after verified termination/postconditions. Windows authority is the Job Object. Unix support in this slice is explicitly the bundled-runner contract: an unreaped leader pins the process-group generation and a Unica-installed child-only inherited lifetime sentinel FD accounts for descendants even after they close stdout/stderr. The pinned runner preserves inherited descriptors but performs no handshake/acknowledgement. Missing/lost capability is ownership-uncertain and quarantines `active.lock`; this does not claim hostile arbitrary-descendant containment.

- [ ] Run red tests:

```bash
cargo test -p unica-coder infrastructure::workspace_index -- --nocapture
cargo test -p unica-coder infrastructure::code_intelligence -- --nocapture
cargo test -p unica-coder infrastructure::runtime_jobs -- --nocapture
cargo test -p unica-coder infrastructure::platform::process -- --nocapture
```

- [ ] Give `WorkspaceActor` an exact Index coordinator and the daemon exact ProviderHost and RuntimeResource coordinators. SharedWork coalesces only in-process ownership: `BslIndexLock`, actor revision/root fences, root-bound provider sessions/caches and runtime `active.lock` remain the stronger authorities. ProviderHost is rootless (`engine + target + capabilities`), while every root request and result remains actor-bound. Runtime sharing requires the already-active exact resource/job lease; it never deduplicates a new destructive invocation from normalized arguments.

- [ ] The production V13 subject handler remains dormant until Task 22. This slice proves the injected canonical capability and durable handoff before wait, but does not claim subject-tool routing or durable progress projection. It persists no SharedWork phase and registers no resume owner; orphaned non-resumable work continues to recover as terminal `failed/interrupted`/`resume_unsupported` without restart.

- [ ] Keep `runtime_jobs.rs` as an internal executor adapter until cutover; remove its public action registry in Task 22.

- [ ] Run:

```bash
cargo test -p unica-coder infrastructure::workspace_index -- --nocapture
cargo test -p unica-coder infrastructure::workspace_services -- --nocapture
cargo test -p unica-coder infrastructure::code_intelligence -- --nocapture
cargo test -p unica-coder infrastructure::runtime_jobs -- --nocapture
cargo test -p unica-coder infrastructure::platform::process -- --nocapture
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/application/shared_work.rs crates/unica-coder/src/domain/source_revision.rs crates/unica-coder/src/infrastructure/daemon/server.rs crates/unica-coder/src/infrastructure/engine_delivery.rs crates/unica-coder/src/infrastructure/platform/filesystem.rs crates/unica-coder/src/infrastructure/platform/mod.rs crates/unica-coder/src/infrastructure/platform/process.rs crates/unica-coder/src/infrastructure/runtime_jobs.rs crates/unica-coder/src/infrastructure/workspace_actor.rs crates/unica-coder/src/infrastructure/workspace_services.rs arch/decisions/2026-08-24-long-work-ownership-slice.md arch/invariants/INV.APP.EXACT-LONG-WORK-OWNERSHIP.md arch/invariants/INV.APP.RUNTIME-RESOURCE-TREE.md arch/contracts/CTR.APP.DAEMON-LONG-WORK-CAPABILITIES.md arch/index.md docs/plans/2026-08-23-v0-12-3-to-v0-13-migration.md
git commit -m "refactor(runtime): unify long work under daemon ownership"
```

## Task 12: Build the Logical Address and Platform Capability Core

**Files:**

- Create: `crates/unica-coder/src/domain/address.rs`
- Create: `crates/unica-coder/src/domain/platform_profile.rs`
- Create: `crates/unica-coder/src/infrastructure/logical_tree.rs`
- Modify: `crates/unica-coder/src/domain/mod.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/meta/mod.rs`
- Create: `tests/fixtures/v013/address-profile-8.3.27.json`

- [ ] Add table-driven failing tests for source-set prefix, alternating kind/name grammar, branch-ending kinds, arbitrary depth, English canonical kinds, proven Russian aliases, and canonical English output.

- [ ] Cover these concrete addresses: configuration root, metadata branch, object, attribute, nested tabular-section attribute, form attribute/parameter/command/element/table/column, role rights/RLS, DCS nodes, MXL area/parameter, and every module role from the approved module design.

- [ ] Add capability tests for 8.3.27: valid missing module is visible; invalid owner/role is `not_found`; Bot and WebSocketClient are present; HTTP, SOAP and IntegrationService remain distinct; gRPC is absent.

- [ ] Run red tests:

```bash
cargo test -p unica-coder domain::address -- --nocapture
cargo test -p unica-coder domain::platform_profile -- --nocapture
cargo test -p unica-coder infrastructure::logical_tree -- --nocapture
```

- [ ] Implement `QualifiedAddress { source_set, segments }`, typed `NodeKind`, alias normalization and `PlatformProfile::v8_3_27()`. Never infer logical existence from a file path.

- [ ] Implement branch routing over current metadata readers without copying their XML parsing. Physical paths may appear only in diagnostic `file`, never as identity.

- [ ] Run:

```bash
cargo test -p unica-coder domain::address -- --nocapture
cargo test -p unica-coder domain::platform_profile -- --nocapture
cargo test -p unica-coder infrastructure::logical_tree -- --nocapture
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/domain/address.rs crates/unica-coder/src/domain/platform_profile.rs crates/unica-coder/src/domain/mod.rs crates/unica-coder/src/infrastructure/logical_tree.rs crates/unica-coder/src/infrastructure/native_operations/meta/mod.rs tests/fixtures/v013/address-profile-8.3.27.json
git commit -m "feat(address): model the complete logical 1C tree"
```

## Task 13: Implement the Module Projection Contract

**Files:**

- Create: `crates/unica-coder/src/domain/module_projection.rs`
- Create: `crates/unica-coder/src/infrastructure/bsl_module_projection.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/form_event_registry.rs`
- Modify: `crates/unica-coder/src/infrastructure/logical_tree.rs`
- Create: `tests/fixtures/v013/modules/`

- [ ] Add failing fixtures and tests for object, manager, record-set, value-manager, common, form, command, managed application, ordinary application, external connection, and session modules plus HTTP, SOAP, IntegrationService, Bot and WebSocketClient.

- [ ] Test a valid module with no file: summary exists with zero source-derived counts for `Method`, `Region`, `Interface`, `Compilation`, and `Body`; `Event.count` equals the applicable 8.3.27 possible-event projection size and may be non-zero. Every branch count equals the actual projection size; first `event.implement` can materialize the module later. Reject `sourceState`, `fileExists`, `set`, and generic role `Service` in serialized output.

- [ ] Test method projection: exact signature, doc block, procedure/function, export, directive, accumulated preprocessor conditions, effective contexts, extension annotation/`targetAt`, multiple `handles`, source order, no body.

- [ ] Test body projection: explicit `.Body`, line-number-preserving pagination, unfiltered original source, context filter for client/server without duplicating the method.

- [ ] Test region semantics, three subsystem-interface projections, declared service handlers, and event registry states `available`, `implemented`, `missing`, `invalid`.

- [ ] Test form event ownership separately for form, element, form-table, nested column and command; method `handles[]` must point back to every binding.

- [ ] Run red tests:

```bash
cargo test -p unica-coder domain::module_projection -- --nocapture
cargo test -p unica-coder infrastructure::bsl_module_projection -- --nocapture
```

- [ ] Implement the six branch projections `Method`, `Region`, `Interface`, `Event`, `Compilation`, `Body` exactly as the approved design. The module summary prints counts and exact common-module flags once; it never includes methods or source text.

- [ ] Use the 8.3.27 profile registry for possible events even when no handler exists. Treat XML `callType` as a binding fact, not a BSL annotation.

- [ ] Run:

```bash
cargo test -p unica-coder domain::module_projection -- --nocapture
cargo test -p unica-coder infrastructure::bsl_module_projection -- --nocapture
cargo test -p unica-coder infrastructure::native_operations::form_event_registry -- --nocapture
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/domain/module_projection.rs crates/unica-coder/src/infrastructure/bsl_module_projection.rs crates/unica-coder/src/infrastructure/native_operations/form_event_registry.rs crates/unica-coder/src/infrastructure/logical_tree.rs tests/fixtures/v013/modules
git commit -m "feat(view): project modules, contexts and event handlers"
```

## Task 14: Implement `view` and `find`

**Files:**

- Create: `crates/unica-coder/src/application/v13/view.rs`
- Create: `crates/unica-coder/src/application/v13/find.rs`
- Create: `crates/unica-coder/src/domain/node_view.rs`
- Create: `crates/unica-coder/src/infrastructure/v13_read.rs`
- Create: `crates/unica-coder/src/infrastructure/v13_read_projection.rs`
- Create: `crates/unica-coder/src/infrastructure/v13_read/tests.rs`
- Create: `crates/unica-coder/src/infrastructure/v13_find.rs`
- Create: `arch/decisions/2026-08-25-logical-read-core-slice.md`
- Create: `arch/contracts/CTR.SOURCE.LOGICAL-NODE-VIEW-SHAPE.md`
- Create: `arch/invariants/INV.SOURCE.LOGICAL-READER-PARITY.md`
- Create: `arch/invariants/INV.SOURCE.REVISION-BOUND-VIEW-CURSOR.md`
- Create: `arch/invariants/INV.SOURCE.FIND-IDENTITY-ONLY.md`
- Modify: `crates/unica-coder/src/application/v13/mod.rs`
- Modify: `crates/unica-coder/src/domain/mod.rs`
- Modify: `crates/unica-coder/src/infrastructure/logical_tree.rs`
- Modify: `crates/unica-coder/src/infrastructure/mod.rs`
- Modify: `crates/unica-coder/src/infrastructure/platform_xml_source_targets.rs`
- Modify: `crates/unica-coder/src/application/result_store.rs`
- Modify: `arch/index.md`

- [ ] Add failing `view` tests for the seven common slots, branch counts, node/data distinction, opaque content visibility, exact cursor revision, cursor invalidation after revision change, and bounded line/item pagination.

- [ ] Add typed-reader parity fixtures for metadata, forms, roles/RLS, subsystems/interface, DCS, MXL, XDTO and modules. Every old reader scenario must map to one logical address.

- [ ] Add failing `find` tests for Russian object name, English kind, synonym, source export path, exact kind filter, limit, nearest candidates and no content/module mixing.

- [ ] Run red tests:

```bash
cargo test -p unica-coder application::v13::view -- --nocapture
cargo test -p unica-coder application::v13::find -- --nocapture
cargo test -p unica-coder domain::node_view -- --nocapture
```

- [ ] Implement `view` as one router over typed projections. `at` depth selects the projection; there are no `depth`, `sections`, `delivery`, `page`, `region`, or `method` arguments.

- [ ] Implement `find` over address/name/path indices. Return candidates with canonical `at`, `kind`, `title`, and match reason; do not return node body or search hits.

- [ ] Add both tool specs only to injected `SurfaceRelease::V13`.

- [ ] Run:

```bash
cargo test -p unica-coder application::v13::view
cargo test -p unica-coder application::v13::find
cargo test -p unica-coder domain::node_view
cargo test -p unica-coder application::v13::tool_catalog -- --nocapture
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/application/v13/view.rs crates/unica-coder/src/application/v13/find.rs crates/unica-coder/src/application/v13/mod.rs crates/unica-coder/src/domain/node_view.rs crates/unica-coder/src/infrastructure/logical_tree.rs crates/unica-coder/src/application/result_store.rs
git commit -m "feat(surface): add logical view and find entries"
```

## Task 15: Implement Transactional Batch `apply`

**Files:**

- Create: `crates/unica-coder/src/domain/apply.rs`
- Create: `crates/unica-coder/src/application/v13/apply.rs`
- Create: `crates/unica-coder/src/infrastructure/native_operations/apply.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/compile_transaction.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations.rs`
- Modify: `crates/unica-coder/src/application/v13/mod.rs`

- [ ] Add failing tests for empty `ops`, source-order execution, one or many data items inside one op, unknown op with `can[]`, bad value with exact `args` address, not-found with nearest addresses, and whole-batch `ifRev` failure.

- [ ] Add atomicity tests: operation 1 stages `event.implement` into a missing module, operation 2 fails validation, and neither module file nor metadata change is published. Repeat for dryRun and successful apply.

- [ ] Add first-call-skeleton tests: `view(...).can[]` names the applicable operation and minimal argument shape; `tools/list` contains only an `op: string` plus args object, not the union.

- [ ] Run red tests:

```bash
cargo test -p unica-coder domain::apply application::v13::apply infrastructure::native_operations::apply -- --nocapture
```

- [ ] Implement `ApplyRequest { at, ops: NonEmptyVec<ApplyOp>, dry_run, if_rev }`. Resolve all logical targets, validate all operations and stage all bytes/preimages before publication. Reuse `CompileTransaction` atomic replace and source revision fence.

- [ ] Register existing metadata/form/role/DCS/MXL/XDTO/subsystem/support/code operations behind noun-dot-verb names. An operation may accept an array when the data is naturally plural; one `apply` call remains one transaction.

- [ ] Implement `event.implement` through the module/event registry so a permitted absent module is materialized without `module.create`.

- [ ] Run:

```bash
cargo test -p unica-coder domain::apply application::v13::apply infrastructure::native_operations::apply infrastructure::native_operations::compile_transaction -- --nocapture
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/domain/apply.rs crates/unica-coder/src/application/v13/apply.rs crates/unica-coder/src/infrastructure/native_operations/apply.rs crates/unica-coder/src/infrastructure/native_operations/compile_transaction.rs crates/unica-coder/src/infrastructure/native_operations.rs crates/unica-coder/src/application/v13/mod.rs
git commit -m "feat(surface): apply ordered operations atomically"
```

## Task 16: Implement `search`

**Files:**

- Create: `crates/unica-coder/src/application/v13/search.rs`
- Modify: `crates/unica-coder/src/application/v13/mod.rs`
- Modify: `crates/unica-coder/src/infrastructure/code_intelligence.rs`
- Modify: `crates/unica-coder/src/domain/code_intelligence.rs`

- [ ] Add a failing parity table mapping current `code.search`, `code.definition` and reference scenarios to `search` without retaining their old schemas.

- [ ] Test content query, regex, logical subtree scope, definitions/references in provider-neutral sections, canonical `at` on every hit, partial coverage, deterministic limit and cold-index Task with the provider retry hint.

- [ ] Run red tests:

```bash
cargo test -p unica-coder application::v13::search infrastructure::code_intelligence domain::code_intelligence -- --nocapture
```

- [ ] Implement the facade over the current provider-neutral code-intelligence port. `scope` resolves before provider dispatch; provider paths are converted back to canonical addresses. Route index readiness through Invocation/SharedWork, not a `working` result field.

- [ ] Run:

```bash
cargo test -p unica-coder application::v13::search infrastructure::code_intelligence domain::code_intelligence -- --nocapture
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/application/v13/search.rs crates/unica-coder/src/application/v13/mod.rs crates/unica-coder/src/infrastructure/code_intelligence.rs crates/unica-coder/src/domain/code_intelligence.rs
git commit -m "feat(surface): search content and symbols by logical scope"
```

## Task 17: Implement `check` With One Validator Registry

**Files:**

- Create: `crates/unica-coder/src/application/v13/check.rs`
- Create: `crates/unica-coder/src/domain/validation.rs`
- Modify: `crates/unica-coder/src/application/v13/apply.rs`
- Modify: `crates/unica-coder/src/application/v13/mod.rs`
- Modify: `crates/unica-coder/src/application/diagnostics.rs`

- [ ] Add a failing parity table mapping project health, code diagnostics and every current `*.validate` scenario to `check`.

- [ ] Test that no address means workspace health, an address checks one node/subtree, findings have stable code and canonical `at`, filters are typed, and a dependency can hand off as Task.

- [ ] Add a spy-validator test proving persisted-state `check` and staged-state `apply(dryRun)` call the same validator implementation with different `ValidationView` values.

- [ ] Run red tests:

```bash
cargo test -p unica-coder application::v13::check domain::validation -- --nocapture
```

- [ ] Implement `ValidatorRegistry` and the two state views. Keep mutation feasibility in apply preflight and existing-state findings in check, while sharing each concrete validation rule.

- [ ] Run:

```bash
cargo test -p unica-coder application::v13::check application::v13::apply domain::validation application::diagnostics -- --nocapture
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/application/v13/check.rs crates/unica-coder/src/domain/validation.rs crates/unica-coder/src/application/v13/apply.rs crates/unica-coder/src/application/v13/mod.rs crates/unica-coder/src/application/diagnostics.rs
git commit -m "feat(surface): check persisted and staged state consistently"
```

## Task 18: Implement Structural `diff`

**Files:**

- Create: `crates/unica-coder/src/application/v13/diff.rs`
- Create: `crates/unica-coder/src/domain/structural_diff.rs`
- Modify: `crates/unica-coder/src/application/v13/mod.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/cfe.rs`
- Modify: `crates/unica-coder/src/infrastructure/logical_tree.rs`

- [ ] Add failing tests for qualified left/right addresses, kind compatibility, source-set and extension comparison, deterministic add/remove/change rows, filter, limit, and cursor bound to both source revisions.

- [ ] Add a no-write test around every diff fixture and a parity case for current `unica.cfe.diff`.

- [ ] Run red tests:

```bash
cargo test -p unica-coder application::v13::diff domain::structural_diff -- --nocapture
```

- [ ] Implement diff over typed logical projections rather than raw XML text. Rows identify the comparable logical location and before/after typed values; opaque bodies use their explicit content projection.

- [ ] Run:

```bash
cargo test -p unica-coder application::v13::diff domain::structural_diff infrastructure::native_operations::cfe -- --nocapture
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/application/v13/diff.rs crates/unica-coder/src/domain/structural_diff.rs crates/unica-coder/src/application/v13/mod.rs crates/unica-coder/src/infrastructure/native_operations/cfe.rs crates/unica-coder/src/infrastructure/logical_tree.rs
git commit -m "feat(surface): diff two logical source states"
```

## Task 19: Implement Provider-Neutral `docs`

**Files:**

- Create: `crates/unica-coder/src/application/v13/docs.rs`
- Modify: `crates/unica-coder/src/application/v13/mod.rs`
- Modify: `crates/unica-coder/src/infrastructure/platform_help.rs`
- Modify: `crates/unica-coder/src/infrastructure/standards_documentation.rs`

- [ ] Add a failing parity table for current documentation and standards search/get/explain scenarios.

- [ ] Test query and exact article identifier, source-kind filter, provenance/version per article, provider replacement without schema change, one provider failure not hiding the others, and cold corpus delivery through Task.

- [ ] Run red tests:

```bash
cargo test -p unica-coder application::v13::docs infrastructure::platform_help infrastructure::standards_documentation -- --nocapture
```

- [ ] Implement the facade over typed documentation providers. Public input knows source kinds, never provider identities; exact articles and search hits use one result envelope with provenance.

- [ ] Run:

```bash
cargo test -p unica-coder application::v13::docs infrastructure::platform_help infrastructure::standards_documentation -- --nocapture
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/application/v13/docs.rs crates/unica-coder/src/application/v13/mod.rs crates/unica-coder/src/infrastructure/platform_help.rs crates/unica-coder/src/infrastructure/standards_documentation.rs
git commit -m "feat(surface): unify documentation behind source kinds"
```

## Task 20: Implement `run` With Daemon Process Ownership

**Files:**

- Create: `crates/unica-coder/src/application/v13/run.rs`
- Create: `crates/unica-coder/src/domain/run.rs`
- Modify: `crates/unica-coder/src/application/v13/mod.rs`
- Modify: `crates/unica-coder/src/infrastructure/internal_adapters.rs`
- Modify: `crates/unica-coder/src/infrastructure/runtime_build_preflight.rs`
- Modify: `crates/unica-coder/src/infrastructure/runtime_jobs.rs`
- Modify: `crates/unica-coder/src/infrastructure/platform/process.rs`

- [ ] Add failing catalog tests: `run()` returns exactly the 13 contract operations with minimal args skeletons and risk/long-running classification; it contains no raw CLI, `tools-download`, job action or detached client.

- [ ] Add one parity fixture for each old `build.*` and admitted `runtime.execute` intention. Explicitly assert the mapping for removed/changed selectors rather than silently forwarding the old payload.

- [ ] Add process acceptance tests: known-long operations immediately become Task, success waits for the full child tree and postconditions, cancel kills the whole owned tree, non-abortable phase fails preflight when the budget cannot reach a safe boundary, and no detached mutation survives daemon shutdown.

- [ ] Run red tests:

```bash
cargo test -p unica-coder application::v13::run domain::run infrastructure::platform::process -- --nocapture
```

- [ ] Implement the typed operation registry. Translate each typed args object to the existing runner adapter only behind the application boundary. Remove raw operation passthrough and make the registry the sole owner of risk, handoff and postcondition metadata.

- [ ] Keep `test.run` in v0.13. Mark its v0.14 move in the operation descriptor data, not by exposing `unica.tests` early.

- [ ] Run:

```bash
cargo test -p unica-coder application::v13::run domain::run infrastructure::runtime_jobs infrastructure::platform::process -- --nocapture
```

- [ ] Commit:

```bash
git add crates/unica-coder/src/application/v13/run.rs crates/unica-coder/src/domain/run.rs crates/unica-coder/src/application/v13/mod.rs crates/unica-coder/src/infrastructure/internal_adapters.rs crates/unica-coder/src/infrastructure/runtime_build_preflight.rs crates/unica-coder/src/infrastructure/runtime_jobs.rs crates/unica-coder/src/infrastructure/platform/process.rs
git commit -m "feat(surface): run typed platform operations in the daemon"
```

## Task 21: Prove All Eight Entries on Direct, Native Task and Compatibility Paths (Gate F5)

**Files:**

- Create: `tests/fixtures/v013/domain-parity.json`
- Create: `tests/ci/test_v013_surface_profiles.py`
- Modify: `scripts/ci/probe-unica-wire.py`
- Modify: `tests/ci/test_probe_unica_wire.py`
- Modify: `crates/unica-coder/src/interfaces/mcp.rs`

- [ ] Add a matrix with one fast and one controlled-long fixture for each of `view`, `apply`, `find`, `search`, `check`, `diff`, `run`, `docs`.

- [ ] For each fixture assert:

  - native Tasks profile fast path returns direct `CallToolResult`;
  - native Tasks profile long path returns `CreateTaskResult` and `tasks/get` reaches terminal;
  - compatibility profile fast path returns the same direct result;
  - compatibility long path returns a receipt and `unica.task.result` reaches terminal;
  - all three terminal domain payloads are byte-identical;
  - execution count equals one;
  - no result contains legacy `job` or `work`.

- [ ] Add daemon restart between receipt and poll for all resumable fixture classes and one non-resumable mutation that deterministically becomes `failed/interrupted`.

- [ ] Add exact surface assertions: Tasks capability → 8 names; no Tasks capability → 11 names; no session/global cache contamination; legacy initialize and direct-first `2026-07-28` both work.

- [ ] Run red matrix before completing missing wiring:

```bash
python3 tests/ci/test_v013_surface_profiles.py -v
python3 tests/ci/test_probe_unica_wire.py -v
```

- [ ] Complete only the wiring exposed by the matrix; do not add per-tool async implementations.

- [ ] Run:

```bash
cargo test -p unica-coder --lib -- --nocapture
python3 tests/ci/test_v013_surface_profiles.py -v
python3 tests/ci/test_probe_unica_wire.py -v
```

- [ ] Commit:

```bash
git add tests/fixtures/v013/domain-parity.json tests/ci/test_v013_surface_profiles.py scripts/ci/probe-unica-wire.py tests/ci/test_probe_unica_wire.py crates/unica-coder/src/interfaces/mcp.rs
git commit -m "test(surface): prove direct and task result parity"
```

## Task 22: Perform the Single Public v0.13 Cutover

**Files:**

- Modify: `Cargo.toml`
- Modify: `Cargo.lock`
- Modify: `plugins/unica/.codex-plugin/plugin.json`
- Modify: `plugins/unica/.claude-plugin/plugin.json`
- Modify: `plugins/unica/third-party/tools.lock.json`
- Modify: `crates/unica-coder/src/application/mod.rs`
- Modify: `crates/unica-coder/src/application/tool_contracts.rs`
- Modify: `crates/unica-coder/src/interfaces/mcp.rs`
- Delete: `crates/unica-coder/src/domain/long_work.rs`
- Modify or delete after reference proof: `crates/unica-coder/src/infrastructure/runtime_jobs.rs`
- Modify: `crates/unica-coder/src/main.rs`
- Create: `arch/contracts/CTR.WIRE.V0-13-DOMAIN-TOOLS.md`
- Modify: `arch/contracts/CTR.WIRE.TOOL-SURFACE.md`
- Modify: `arch/tool-surface-review.json`
- Regenerate: `arch/tool-surface.md`
- Modify: `arch/invariants/INV.APP.HIDDEN-SERVICES.md`
- Modify: `arch/invariants/INV.OBS.RUNTIME-JOB-SURFACE.md`
- Modify: `arch/invariants/INV.OBS.DETACHED-JOB-STATE.md`
- Modify: `arch/invariants/INV.OBS.WAIT-TIMEOUT-KEEPS-JOB.md`
- Modify: `arch/invariants/INV.WIRE.DIRECT-FIRST-LIFECYCLE.md`
- Create: `arch/invariants/INV.WIRE.CAPABILITY-GATED-TASK-SURFACE.md`
- Create: `arch/invariants/INV.WIRE.SERVER-DIRECTED-TASK-HANDOFF.md`
- Create: `arch/invariants/INV.APP.INVOCATION-RESULT-PARITY.md`
- Create: `arch/invariants/INV.APP.DURABLE-TASK-RECOVERY.md`
- Create: `arch/invariants/INV.APP.GLOBAL-DAEMON-WORKSPACE-ISOLATION.md`
- Create: `arch/decisions/2026-08-23-v0-13-execution-evidence.md`
- Modify: `arch/decisions/2026-08-20-long-work-answers-with-state.md`
- Modify: `arch/decisions/2026-08-23-v0-13-execution-surface.md`
- Modify: `arch/decisions/2026-08-23-module-contract.md`
- Regenerate: `arch/index.md`

- [ ] First add failing product-contract tests asserting package version `0.13.0`, exact 8/11 profiles, absent old names, absent legacy result slots, no old worker CLI, and active architecture evidence.

- [ ] Run red tests:

```bash
python3 tests/ci/test_product_contracts.py -v
python3 tests/ci/test_tool_surface_ledger.py -v
python3 tests/ci/test_v013_surface_profiles.py -v
```

- [ ] Change workspace/plugin/tool-lock versions together to `0.13.0`. Make `SurfaceRelease::from_package_version()` select V13; remove any test-only path that could override production selection.

- [ ] Replace `tools()` production registry with the eight V13 entries and capability-dependent three compatibility entries. Delete all 71 old public tool registrations and six runtime job schemas; do not retain aliases.

- [ ] Remove `OperationResult.job`, `OperationResult.work`, `domain/long_work.rs`, `--runtime-job-worker`, and the obsolete per-workspace service mode after `rg` proves their remaining references are only migration fixtures. Keep reusable internal executor code under daemon-owned names.

- [ ] Key tool-definition caches by `SurfaceProfile`; direct-first pagination remains bounded and traverses exactly the selected profile.

- [ ] Activate `DEC.2026-08-23.MODULE-CONTRACT` by changing only `status` and `realized`. The execution direction needs new derived rules, while the V2 registry forbids adding `establishes` during planned→active realization; therefore create active `DEC.2026-08-23.V0-13-EXECUTION-EVIDENCE` with named test evidence, make it supersede both the planned execution direction and `DEC.2026-08-20.LONG-WORK-ANSWERS-WITH-STATE`, and mark both predecessors superseded by it.

- [ ] Let the evidence decision establish `CTR.WIRE.V0-13-DOMAIN-TOOLS`, the five new invariants and the revised `INV.WIRE.DIRECT-FIRST-LIFECYCLE`. Mark old job invariants and `INV.APP.HIDDEN-SERVICES` superseded. Update `CTR.WIRE.TOOL-SURFACE` version/decision and create the active domain contract with concrete check symbols from Tasks 1, 5, 7, 8, and 21. Do not hand-edit generated `arch/tool-surface.md`.

- [ ] Rewrite every shipped skill that calls a removed tool so it routes through the eight entries. Preserve specialized 1C guidance, but remove direct references to old tool names, `runtime.job.*`, `jobId`, and repeated-call `working` continuation.

- [ ] Run the complete cutover suite:

```bash
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace --all-targets -- --nocapture
python3 scripts/arch/registry.py --write-index
python3 scripts/arch/registry.py --check
python3 scripts/arch/immutability.py --base origin/main
python3 scripts/ci/generate-tool-surface.py
python3 -m unittest discover -s tests/ci -p 'test_*.py' -v
python3 -m unittest discover -s tests/arch -p 'test_*.py' -v
```

- [ ] Inspect `git diff --name-status origin/main...HEAD` and prove there is no accidental deletion outside obsolete surface/runtime adapters.

- [ ] Commit the cutover as one commit:

```bash
git add -A
git commit -m "feat!: switch Unica to the v0.13 execution surface"
```

## Task 23: Prove Packaging, Upgrade, Rollback and Offline Operation (Gate F6)

**Files:**

- Modify: `scripts/ci/package-unica-runtime.py`
- Modify: `scripts/ci/package-unica-plugin.py`
- Modify: `scripts/ci/smoke-unica-bootstrap.py`
- Modify: `scripts/ci/smoke-unica-mcp.py`
- Modify: `scripts/ci/verify-release-assets.py`
- Modify: `scripts/ci/verify-delivery-reachable.py`
- Modify: `.github/workflows/unica-plugin-release.yml`
- Create: `tests/ci/test_v013_migration_acceptance.py`
- Modify: `docs/release-runbook.md`
- Modify: `README.md`

- [ ] Add failing packaged acceptance tests for all 24 items in the execution-surface design. Give each test a stable name `acceptance_01_...` through `acceptance_24_...` so architecture evidence can cite exact symbols.

- [ ] Add release-package assertions:

  - the marketplace package contains core/bootstrap, not a v0.12-style full runtime;
  - first core-only `tools/list` performs no engine download;
  - first engine-requiring call returns/joins Task and downloads one pinned artifact;
  - `prefetch` makes all eight entries usable offline;
  - a v0.12.3 cache is not mistaken for v0.13 core/engine state;
  - rollback v0.12.3 still starts from its untouched namespace;
  - daemon endpoint identity splits incompatible versions;
  - Windows, Linux and macOS package metadata keep one `unica` MCP server.

- [ ] Run the red acceptance suite against an intentionally incomplete package to prove the tests detect old full-runtime delivery and wrong surface.

- [ ] Update package/smoke scripts and workflow only where the failing evidence requires it. Preserve the release-runbook gate: immutable native assets, checksums, licenses/provenance, package smoke, then catalog promotion.

- [ ] Build release-candidate packages for the current target and CI targets. Run both profiles through `probe-unica-wire.py`; record generated reports as CI artifacts, not tracked architecture truth.

- [ ] Run:

```bash
python3 tests/ci/test_v013_migration_acceptance.py -v
python3 tests/ci/test_package_unica_runtime.py -v
python3 tests/ci/test_package_unica_plugin.py -v
python3 tests/ci/test_smoke_unica_bootstrap.py -v
python3 tests/ci/test_smoke_unica_mcp.py -v
python3 tests/ci/test_verify_release_assets.py -v
python3 scripts/ci/package-unica-plugin.py --help
python3 scripts/ci/verify-delivery-reachable.py --help
```

- [ ] Perform live packaged-host probes on current released Codex and Claude Code. Expect compatibility profile until a host actually declares SEP-2663; when one does, expect native 8-tool profile without changing the package.

- [ ] Update README and release notes with an explicit migration table: every old tool family maps to `view`, `apply`, `find`, `search`, `check`, `diff`, `run`, or `docs`; `runtime.job.*` maps to protocol Tasks or compatibility task tools, not an alias.

- [ ] Commit release-candidate proof:

```bash
git add scripts/ci .github/workflows/unica-plugin-release.yml tests/ci/test_v013_migration_acceptance.py docs/release-runbook.md README.md
git commit -m "test(release): prove the v0.13 migration package"
```

## Task 24: Final Semantic Review and Release Handoff

**Files:**

- Review only: all changes since Task 1
- Create during execution only if the repository convention requires it: release notes through the existing generator

- [ ] Review the full diff against both approved designs, not only CI. Confirm all 15 module-contract criteria and all 24 execution-surface criteria have named executable evidence.

- [ ] Search for forbidden leftovers:

```bash
rg -n 'unica\.(runtime\.job|build\.|cf\.|cfe\.|code\.|documentation\.|dcs\.|form\.|interface\.|meta\.|mxl\.|project\.|role\.|source\.|standards\.|subsystem\.|support\.|xdto\.)' plugins/unica README.md crates/unica-coder/src --glob '!**/tests/**'
rg -n 'jobId|sourceState|fileExists|"set"|OperationResult\.job|OperationResult\.work|--runtime-job-worker|--workspace-service' crates plugins README.md
```

Expected: no public/production reference; explicit migration prose and baseline fixtures are allowed and reviewed individually.

- [ ] Run final clean verification from a fresh target/cache directory:

```bash
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace --all-targets
python3 -m unittest discover -s tests/ci -p 'test_*.py' -v
python3 -m unittest discover -s tests/arch -p 'test_*.py' -v
python3 scripts/arch/registry.py --check
python3 scripts/arch/immutability.py --base origin/main
git status --short
```

- [ ] Re-query issues #465, #497, #512, #563, #566, #572, #574 and #581. Close only acceptance already proved by merged code. Keep #563 open for v0.14 and explicitly state that v0.13 has 8/11 profiles while v0.14 is planned for 11/14.

- [ ] Prepare one independently reviewable PR against `main`. Do not base it on another open PR. Publication/tag remains a separate owner-approved action following `docs/release-runbook.md`.

## Acceptance Traceability

| Accepted architecture evidence | Implemented/proved in |
| --- | --- |
| Exact v0.12.3 74/6 baseline | Task 2 |
| Exact v0.13 8/11 profiles | Tasks 9, 18, 19 |
| Ordinary call, server-selected native Task | Tasks 7–8 |
| 7 000 ms and known-long immediate handoff | Task 7 |
| Same direct/Task/compatibility result | Tasks 3, 8, 18 |
| Durable recovery, TTL, cancel | Tasks 4, 7–9, 18 |
| One user/core daemon and lifecycle | Task 5 |
| Worktree isolation and multiroot provider | Tasks 6, 11 |
| Exact delivery/index/provider/runtime SharedWork | Tasks 10–11 |
| Core-first fresh install, upgrade, rollback, offline | Task 23 |
| Complete logical addressing | Task 12 |
| Module roles, methods, contexts, events and handlers | Task 13 |
| Compact `view` and address-only `find` | Task 14 |
| Atomic batch `apply` and `event.implement` | Task 15 |
| `search` boundary and index readiness | Task 16 |
| `check` and shared validation | Task 17 |
| Structural `diff` | Task 18 |
| Provider-neutral `docs` | Task 19 |
| Typed daemon-owned `run` | Task 20 |
| No partial public surface | Task 22 |
| Architecture V2 active/realized evidence | Tasks 22–24 |

## Stop Conditions

Stop implementation and return to architecture review if any of these occurs:

- an old scenario cannot be assigned to exactly one of the eight entries;
- a host requires a different public schema rather than a capability-dependent profile;
- durable recovery would require storing raw secrets or blindly replaying a mutation;
- a platform writer cannot stage the whole `apply` batch before publication;
- a provider can only be shared by merging WorkspaceActor state;
- a release artifact needs a second public MCP server or second independently versioned executable;
- the packaged matrix cannot distinguish v0.12.3 full-runtime cache from v0.13 core/engine cache.

Such a stop is an architecture finding. It requires a successor `DEC.*` and derived `INV.*`/`CTR.*`, not a local exception in code.
