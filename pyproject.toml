[project]
name = "unica-dev"
version = "0.0.0"
description = "Development tooling for the Unica repository"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false
