#!/usr/bin/env bash
# Исследование: корпус XML публичного инструмента для платформы 8.3.27.
#
# Вопрос: какие байты платформа 8.3.27 (экспорт 2.20) пишет для каждого вида
# объекта, чтобы наш писатель совпадал с ней побайтно.
# Метод: набор EXECUTABLE_CASES прогоняется через публичный инструмент 1С и
# складывается в UNICA_XML_CORPUS_DIR с манифестом версии 2.
# Входы: установленная платформа 8.3.27; UNICA_XML_CORPUS_DIR — каталог вне
# дерева репозитория. Результат закрепляется корпусом в дереве и записью в
# реестре; страж корпуса — обычный small-тест.
set -euo pipefail
cd "$(dirname "$0")/../.."
: "${UNICA_XML_CORPUS_DIR:?задайте каталог для корпуса вне дерева репозитория}"
cargo test -p unica-coder --features research --test research_xml_corpus -- --exact generate_platform_xml_corpus --nocapture
